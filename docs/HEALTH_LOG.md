# HEALTH_LOG — platform health review history

> The auditable record of every Layer-2 platform-health review (see
> [HEALTH_MONITORING.md](HEALTH_MONITORING.md) + [PHASE_3_HEALTH_PLAN.md](PHASE_3_HEALTH_PLAN.md)).
> One row per flagged board per review: what `core/health.py` flagged, the live diagnosis,
> the catalog fix applied (through the validator — never an ad-hoc scanner edit), and the PR.
> This is how board-rot repair stops being invisible tribal knowledge and becomes a trend we
> can see: a board that recurs here is a demotion candidate; a signal that keeps mis-firing is a
> threshold to tune.

## How a review works (the cue → fix loop)

1. A scan prints `⚠ platform health review due`. That's the cue.
2. Run `python3 core/health.py [--profile <id>]` — it flags boards mechanically and acks the counter.
3. For each finding, **fetch a live sample and diagnose** the real cause (selector drift? moved
   endpoint? renamed slug? new bot-wall? empty config?).
4. Apply the fix in `catalog/platforms.yaml` (HARVEST_SPEC / quirk / `fetch_mode` / `active` /
   `status` / ATS tokens) and prove it with `python3 core/validate.py`.
5. **Generalize before moving on (standing rule, added 2026-07-21 — Borjan: "any improvement
   found needs to be checked how it applies to all platforms, not just this session").** A
   fix is rarely platform-specific in shape, only in the specific board it was found on. Before
   closing out a board, ask: does this SAME pattern apply to any other board — already reviewed
   this session, or not yet reached? Concretely, for every fix, check:
   - **Stream coverage**: does the board's `categories` dict cover every stream its actual fetch
     mechanism already supports, or is it under-declaring (2026-07-21: Dynamite Jobs' sitemap
     rebuild only served 2 of 12 streams by declared category, even though its site has ~19
     matching parent categories; WWR's `categories` dict was gating 4 streams a board with NO
     per-category fetch — one unconditional all-jobs RSS — never actually needed to gate)?
   - **Region/scope coverage**: does the profile's `search.regions_acceptable` (or any other
     profile-level scope) actually match what the board is being fetched for (2026-07-21:
     Jobgether's EMEA-only URL undercounted a worldwide-accepting profile ~10x)?
   - **Fetch completeness**: does the fetch actually reach everything the board has (pagination,
     batching, a public sitemap that sidesteps rendering entirely), or does it silently stop
     early (2026-07-21: Jobgether page-1-only; Dynamite Jobs' scroll-batch limit; Himalayas'
     2026-07-20 pagination-cap bug)?
   If a check above surfaces something for an ALREADY-reviewed board, go back and fix it in the
   same session/PR rather than deferring — don't let "already marked good enough" freeze a fix
   that's now known to apply. This is standing discipline for every future platform-health
   session, not a one-time pass over today's findings.
6. Add a row below. Link the PR. Flip the board's `status` back to `active`/`verified` only once a
   live fetch produces sane, structured results.

**Signal → likely fix cheat-sheet:**

| Signal | Most common real cause | Typical catalog fix |
|--------|------------------------|---------------------|
| `SELECTOR_SUSPECT` | markup/HTML changed under a HARVEST_SPEC | update the `href` regex / `company_idx` / `min_hyphens`, or flip `fetch_mode` to headless |
| `DOWN_STREAK` | endpoint moved, new bot-wall, or board gone dark | new URL/api, add a mirror, escalate fetch_mode, or demote/deactivate |
| `YIELD_COLLAPSE` | pagination broke, or a category slug renamed | fix the cursor/slug/category id in `params` |
| `NEVER_PRODUCED` | empty ATS token list, or wrong slug for this profile's stream | populate `ats_boards`, or correct the stream mapping |
| `SYSTEMIC` | **not board rot** — network/proxy/DNS at scan time | none — re-run when connectivity is restored |

## Baseline (build landed)

_2026-07-20 — Platform Health & Self-Healing shipped (PR #52 + #53). **First real review ran the
same day** (borjan-pm, residential IP, 2026-07-20 PM scan) — see the Review log below. Result: 6
`NEVER_PRODUCED` findings, no `SELECTOR_SUSPECT`/`DOWN_STREAK`/`YIELD_COLLAPSE`/`SYSTEMIC`. Notably,
JustRemote + Welcome to the Jungle no longer read as `DOWN_STREAK` (a blocked outage) now that real
`http_ok` telemetry exists — they're `http_ok: true, raw: 0`, i.e. the reached-but-empty shape,
correctly reclassified. Left open for a live-fetch diagnosis (row below)._

## Low-yield sweep — is a small nonzero count hiding a bug? (2026-07-20)

Borjan asked the right follow-up question after the JustRemote fix: the two bugs found there
(dropped category + regex mismatch) silently produced 0, which `health.py`'s `NEVER_PRODUCED`
signal could at least flag. **But what about boards reporting a small, nonzero count forever —
1, 4, 6 — with no prior higher baseline to "collapse" from?** `YIELD_COLLAPSE`/`SELECTOR_SUSPECT`
structurally can't catch that shape (see the new "known blind spot" note in
[HEALTH_MONITORING.md](HEALTH_MONITORING.md)). Manually live-audited every board from that shape
in the last real scan's ledger:

| Board | Reported | Live audit | Verdict |
|-------|----------|------------|---------|
| Arc.dev | 6 | Checked all `/remote-jobs/*` hrefs on the page; only 6 real `/details/` postings exist, everything else is category-tag nav; no pagination/total-count signal found. | **Genuine — no bug.** |
| Himalayas | 4 | **Bug found**: `?limit=100` is now silently capped at 20 server-side (verified: limit 20/50/100/200 all return exactly 20 jobs; `totalCount` still ~99.7k). The old `offset={page*100}` loop assumed 100/page, so it was skipping 80 real jobs between every request (fetched ranks 0-19, then jumped to 100-119, missing 20-99 — a sparse gap-filled sample, not a true newest-1000 sweep). | **Fixed** — see below. |
| Dynamite Jobs | 1 | **Bug found**: most job cards render their link as `<h2 href="...">`, not a real `<a>` tag — a non-standard pattern `_harvest_links()`'s `tree.css("a")` structurally can't see. 15 of 16 real postings were invisible to the harvester. | **Fixed** — see below. |
| Landing.jobs | 1 | Checked all `/at/...` hrefs with and without scroll (4 rounds) — genuinely only 1 real posting for this query either way. | **Genuine — no bug.** |
| Crossover | 1 | Checked with/without scroll, and every plausible PM-adjacent category (`product-management`, `tpm`, `engineering-management`, `services-leadership`) — each shows at most 1 real `/jobs/{id}/...` link. The catalog's "all" strategy (broad hub fetch + downstream keyword filter, same shape as Himalayas/WWR) is the right design; genuinely low current inventory, not a fetch bug. | **Genuine — no bug.** |

**Two real, generic bugs found and fixed, both verified live end-to-end:**

- **Himalayas** — `core/fetch_boards.py`'s `fetch_himalayas()` pagination corrected to the real
  20-per-page size (`offset={page*20}`, was assuming 100/page). Recovered **18 keyword matches
  vs. the buggy 4 (4.5x)** for borjan-pm's PM stream over a fixed newest-1000 window, including
  much more clearly on-target titles ("Scrum Master / Agile Coach", multiple "Project Manager"/
  "Program Manager"). This affects **every profile using Himalayas** (Tier 2, broadly active),
  not just PM. **Follow-up finding (Borjan asked "you crawled all 100k?"):** no — by design, only
  the newest ~1000. Checked how far back that window actually reaches: **~9.8h** at the site's
  observed ~100 jobs/hour rate. Real borjan-pm scan gaps ranged **5-98h** — meaning a fixed
  1000-job window left a genuine blind spot on any gap longer than ~10h (postings never fetched
  at all, not just undercounted). Checked for server-side category/query filtering on the API
  first (`category`, `q`, `query`, `search`, `keyword` params all silently ignored — same "extra
  params dropped" pattern as the `limit` bug; the category HTML page also 403s even via
  headless) — no filtering available, so the only lever is fetching more of the unfiltered feed.
  **Made adaptive** instead of guessing a fixed constant: `scan.py` now computes the real elapsed
  gap since the last scan (read-only peek at `runs.json` before this run's own entry is written)
  and threads it through as `cfg["scan_gap_hours"]`; `fetch_himalayas()`'s new pure
  `himalayas_pages_for_gap()` sizes the page count to that actual gap (1.5x safety margin, floor
  50 pages/~10h for a fresh profile, ceiling 300 pages/~60h so a multi-day skip can't blow up
  scan time unbounded — verified live: a 9.8h gap now pulls 74 pages / 27 matches, better than
  the flat-1000 test's 18). Unit-tested (`tests/unit_fetch_helpers.py`, 7 cases: floor, ceiling,
  monotonic scaling, negative-input safety).
- **Dynamite Jobs** — `_harvest_links()`'s anchor selector widened from `tree.css("a")` to
  `tree.css("[href]")` (any element carrying an href, still filtered by the same per-platform
  regex, so no new noise). Recovered **16/16 vs. the buggy 1** for the same page. This is a
  **shared-function fix** — it benefits any board using the generic harvester that has (or ever
  develops) this same non-`<a>`-href pattern, not just Dynamite Jobs.

## QA findings (pre-production, cloud-IP test scan — not a real review)

_2026-07-20 — a full live `scan.py --profile borjan-pm` run from this build environment (cloud IP,
not Borjan's residential one) proved the telemetry pipeline end-to-end and surfaced two real
findings before any production data existed. **State was restored afterward** (cloud-IP telemetry
doesn't belong in the real baseline) — these are QA notes, not a Layer-2 review row._

- **JustRemote and Welcome to the Jungle answered with a plain HTTP 200 and harvested 0 postings**
  — no connection error at all. Previously read only as `sources_down` (implying a blocked/unreachable
  outage); the real shape is reached-but-empty, i.e. exactly what `SELECTOR_SUSPECT` is designed to
  catch once a rich trailing baseline exists (it's currently reading `NEVER_PRODUCED` instead,
  since the pre-existing history has no runs with `raw > 0` to baseline against — the signal is
  behaving correctly on the data it has). **Re-check on the first real production review**: these
  two may be selector drift, not a bot-wall, and worth a live fetch to confirm.
- **A heal that didn't heal.** Remote Rocketship (worldwide + Europe) escalated direct→headless
  after a `403`, got a real body back, but still harvested **0** candidates — yet the original code
  reported `✚ healed[…]: recovered via headless`, which overstates what happened (reachability, not
  recovery). **Fixed same-day** in `fetch_boards.py`'s `fetch_html_listing`: a heal is now only
  claimed when the escalation *also* recovers ≥1 candidate; otherwise it's logged as `escalated to
  headless for …, still 0 harvested` — an honest non-recovery, not a false heal claim.

## Review log

| Date | Profile | Board | Signal | Live diagnosis | Fix applied | PR |
|------|---------|-------|--------|----------------|-------------|----|
| 2026-07-20 | borjan-pm | Greenhouse / Lever / Workable / Pinpoint HQ (ATS boards) | NEVER_PRODUCED | Not a fault — confirmed config gap: profile's `ats_boards` token lists are genuinely empty for all four (`greenhouse: []`, `lever: []`, `workable: []`, `pinpoint: []`), so there's nothing to fetch yet. `http_ok: true`, `raw: 0` for each, consistent with "no boards configured" (the fetcher's own honest note). | None needed yet — latent coverage opportunity, not rot. Populate `ats_boards` with real company tokens when available (per HEALTH_MONITORING.md "Known current degradations"). | — |
| 2026-07-20 | borjan-pm | JustRemote | NEVER_PRODUCED | **FIXED — the lazy-load diagnosis was wrong.** Two dead-end rounds first: (1) direct+headless on the profile's category URL, plus the renamed-category theory (`manager-exec`) — both showed 0 postings, seemingly confirming the pre-existing 2026-07-11 lazy-load quirk; (2) after Borjan approved a scoped `render.py` `scroll_rounds` capability, re-tested with `window.scrollTo` and a real `mouse.wheel` event — still 0. **Borjan then asked to check `https://justremote.co/remote-jobs` directly** — that surfaced the real cause: their nav dropped the "project-management" category (successor is `manager-exec`), AND postings now use a relative href with no leading slash (`remote-manager-exec-jobs/product-owner-...`) instead of the old absolute `/remote-jobs/{category}/{slug}` — the `HARVEST_SPEC` regex never matched it, so every prior check silently harvested 0 regardless of whether content existed. Plain headless (no scroll needed at all) now yields real postings. | **Two-part catalog fix**: (1) `categories.project-management` updated from `project-management` to `manager-exec`; (2) `HARVEST_SPECS['justremote']` regex updated to match the new relative href shape (`min_hyphens: 2` filters out nav noise like `remote-jobs/new`). **Verified live end-to-end**: `raw: 17, source_down: False, http_ok: True`, incl. genuinely PM-adjacent roles ("Principal Product Manager, AI Custom Models" @ Gitlab, "Product Owner, D365 Finance & Operations" @ Nutrafol). 0/17 happened to keyword-match borjan-pm's specific PM/Scrum-Master title set *this run* — expected (small, mixed-bag category; today's snapshot just didn't have one), not a bug — keyword substring-matching on the concatenated title was independently confirmed still correct. **Known minor cosmetic issue** (not blocking): company + title concatenate without a separator (two adjacent DOM elements, no whitespace between them) — doesn't affect dedup (URL-keyed) or keyword matching; left as a documented quirk rather than building a bespoke parser unprompted. The `scroll_rounds` capability from round 2 turned out not to be the actual fix here, but stays built for a genuine future lazy-load case. | #55 |
| 2026-07-20 | borjan-pm | Welcome to the Jungle | NEVER_PRODUCED | **Live-diagnosed, genuinely new (no prior quirk existed).** Fetched direct + headless for the profile's filtered query URL AND the bare `/jobs` listing with no query at all — **all four return only the marketing/app shell**, 0 job or company links, near-identical byte size regardless of query. No `__NEXT_DATA__`, no embedded Algolia app-id/search-key despite the URL's Algolia-InstantSearch-shaped query params (`refinementList[...][]=`) — the search now appears to run entirely client-side against an endpoint not exposed in page source. This is an architecture change, not a selector/regex fix. | **Quirk note added** to `catalog/platforms.yaml` documenting the finding (no fetch-behavior change — `active: true`/`fetch_mode: direct` unchanged, since a static/snapshot fetch genuinely can't reach the content right now regardless of mode). Flagged as needing either a longer-interaction headless pass (accept cookie-consent, wait for the search XHR) or their internal search API reverse-engineered — left for a future session, not guessed at here. | PR pending |
| 2026-07-21 | borjan-pm | Jobgether | **none — manual audit, not a health.py flag.** Exactly the documented "known blind spot" (HEALTH_MONITORING.md): `raw: 66` read as plausible with no prior higher baseline to collapse from, so `YIELD_COLLAPSE`/`SELECTOR_SUSPECT` structurally couldn't catch it. | **Round 1** — Borjan manually opened the 3 EMEA category URLs and reported live counts (156/14/4) far above what the scanner logged. Live-diagnosed: the generic single-fetch harvester only ever hits page 1; Jobgether paginates at 50/page via `?page=N`. **Round 2** — Borjan then asked to also check the "anywhere" (non-EMEA) variant. Found a THIRD, much bigger listing: the bare `/remote-jobs/{category}` URL (no location prefix) is a separately-served worldwide feed, not a superset view of the EMEA one (`project-manager` emea=156 vs worldwide=1,648). Cross-checked against `profile.yaml`: borjan-pm's `search.regions_acceptable` already includes `worldwide` — the EMEA-only fetch was a real, unintentional coverage gap, not a deliberate scope choice. **A second live-diagnosis finding while sizing pagination for the bigger worldwide feed**: Jobgether's own embedded `total`/`maxPages` counters (round 1's fix trusted `maxPages`) are independently unreliable — `maxPages` undercounts the worldwide listing's real page count (claimed 10, manually confirmed distinct content through page 33, empty at 34), and `total` is flat-out wrong for some slugs (`scrum-master` reported 134,141 total jobs — a site-side bug, matching a number Borjan had separately seen and flagged as implausible). | **Round 1**: new dedicated `fetch_jobgether()` walking `?page=2..maxPages` (trusted the site's counter). **Round 2 (supersedes round 1's page-count logic)**: replaced counter-trusting pagination with a content-driven stop rule — `jobgether_should_continue()` (pure, unit-tested in `tests/unit_fetch_helpers.py`) keeps paginating only while a page still returns content AND at least one link not already collected, capped at `JOBGETHER_MAX_PAGES=40`. Added a new generic, opt-in `region_urls` catalog mechanism (`profile_loader.region_url_patterns()`, unit-tested in `tests/unit_profile_loader.py`) resolved per-profile against `search.regions_acceptable` — prefers `worldwide` when a profile accepts it, else the profile's own first accepted region, else (no `regions_acceptable` set at all) leaves the platform's plain `url_patterns` completely untouched, so every profile that never expressed a region preference (most demo profiles) stays byte-identical. Wired Jobgether's `region_urls` (`worldwide` / `emea`); a category slug with no dedicated worldwide landing page (`agile-delivery-manager`) 301s to `/search-offers?role=...`, handled for free since `requests` follows redirects transparently. **Verified live end-to-end**: borjan-pm raw 66 -> 3,530 across the 3 worldwide categories (~77s fetch time, up from near-instant — noted in the catalog quirk as a scan-wall-clock tradeoff), sampled titles genuinely on-topic; a non-worldwide profile (`demo-fe-react`, no `regions_acceptable` set) independently re-checked to confirm its resolved URLs are unchanged (still `emea/...`). Caught and fixed one bug in this same round before it shipped: the first `region_url_patterns()` draft defaulted an empty `regions_acceptable` to "first declared region" instead of "no preference, don't touch `url_patterns`" — live-checking `demo-fe-react`'s resolved URLs (not just borjan-pm's) caught it silently switching to worldwide too, which would have been an unintended behavior change for every profile that never asked for one. | #63 |
| 2026-07-21 | borjan-pm | Dynamite Jobs | **none — manual audit, not a health.py flag.** `raw: 16`, no `source_down`, no prior baseline to collapse from — same blind-spot shape as Jobgether above, PLUS the count was actively wrong in a way no current signal could catch (see diagnosis). | Borjan manually opened the platform and reported the real category URL is `/remote-jobs/management-operations/project-manager`, not the catalog's `/remote-jobs/project-management`; also flagged that location is a filter with no URL effect, and that postings load in scroll-triggered batches. Live-diagnosed: (1) the catalog's category URL **301-redirected to the homepage** — confirmed by rendering both and comparing: 16 links harvested from each, but **0 URL overlap** between them, meaning the "raw: 16" the ledger had been reporting was never project-management-scoped content at all, just whatever 16 postings happened to be on the homepage's first batch. (2) Headless render at `scroll_rounds=0/3/8` all plateaued at exactly 16 on the CORRECT category page too — confirms Borjan's "loads in batches" read; a plain render (even with scrolling) only ever captures the first batch. (3) Found a better path entirely: `robots.txt` advertises `/sitemap/jobs-categories-index.xml` + `/sitemap/jobs-of-category.xml?category=...`, a public sitemap enumerating EVERY live posting in a parent category (~19 parent categories total) with `lastmod` dates and usually a `"Company - Title"` caption — no scrolling, no headless rendering, no batch-size guessing needed at all. Verified live: 125 postings for `management-operations` via the sitemap vs. the ~16 batch-limited (and wrongly-scoped) render. | Replaced the headless-HTML approach entirely with a new dedicated `fetch_dynamite()` (`core/fetch_boards.py`) hitting the sitemap directly; pure caption-parsing helper `dynamite_title_company()` unit-tested (`tests/unit_fetch_helpers.py`, 4 cases incl. a title that itself contains `" - "` and a missing-caption fallback). Catalog `categories` remapped from browsable subcategory slugs to Dynamite's PARENT taxonomy (`management-operations`, `development`) — a broad bucket like Himalayas/WWR/Jobgether-worldwide, profile's keyword filter narrows downstream. Removed the now-dead `HARVEST_SPECS['dynamite']` entry (the generic HTML harvester is fully bypassed by the new HANDLERS registration). `fetch_mode` corrected from `headless` to `api` to match reality. Confirmed Borjan's location-filter observation live (selecting it doesn't change the URL — no server-side param exists) and documented it as a quirk rather than chasing a fetch fix for something that doesn't exist server-side. **Verified live end-to-end**: raw 16 (wrong-page) -> 125 (correct, complete) for borjan-pm's PM stream; `posted_at` now populated from the sitemap's `lastmod` (a bonus the old harvest never had). **Follow-up same session — Borjan asked "is this fixed for all templates/roles/subvariants?"**: no, the fix only covered the 2 streams already wired (project-management, software-engineering — confirmed those apply uniformly across every subvariant within them, since categories are keyed by stream not subvariant). But the sitemap index (`/sitemap/jobs-categories-index.xml`) lists ~19 parent categories, several with clean matches for MK templates Dynamite had ZERO coverage for before: `data-analyst` (data), `design` (design-ux), `marketing` (marketing), `hr-recruiter` (people-hr), `writing-editing` (content-writing), `business-development` (sales), `customer-support`+`technical-support` (it-support, as a 2-slug list — same list-expansion pattern as Jobgether's PM categories). Each live-verified before wiring (HTTP 200, plausible per-category posting counts 22-183, sample titles spot-checked on-topic) rather than guessed from the slug name alone. `demo-data-engineer`/`demo-product-designer` (previously Dynamite-inactive, stream-gated) now resolve and fetch live; `demo-ai-engineer`/`demo-automation-sdet` correctly remain inactive — no `ai-ml`/`qa` match exists on the platform, a genuine coverage gap, not a config omission. | #63 |
| 2026-07-21 | borjan-pm | We Work Remotely + Jobgether (retroactive, standing-rule pass) | **none — Borjan's process instruction, not a health.py flag.** "We need to go back to all platforms we passed... any improvement found needs to be checked how it applies to all platforms. That should be a rule, not just for this session but for always." | Applied the new step-5 checklist (see above) to the 2 boards already marked "good enough" this session. **WWR**: its fetcher has pulled the unconditional all-jobs RSS since before this session (`{category}` isn't even in `url_patterns` anymore, per the earlier WWR fix) — its `categories` dict was pure leftover stream-gating from before that design, silently skipping `data`/`people-hr`/`content-writing`/`product-management` even though the SAME unfiltered fetch already covers them. **Jobgether**: its landing-page sitemap (`/sitemap/index-landings.xml`, ~4,400 role/skill/location pages) has real pages for far more than the 2 streams wired so far. | **WWR**: `categories: {}` (matches the precedent already used for category-less ATS boards — Greenhouse/Lever/Workable/Pinpoint) — confirmed live `demo-data-engineer` flips inactive→active for WWR with the identical unfiltered feed. **Jobgether**: live-verified (HTTP 200, ~49-50 harvested links each, both worldwide and emea variants) and wired all 10 remaining streams through the SAME `fetch_jobgether`/`region_urls` machinery, no new code: `data`, `design`→design-ux, `marketing`, `sales`, `human-resources`→people-hr, `content-writing`, `it-support`, `ai-and-machine-learning`→ai-ml, `qa`, `product-management`. Confirmed live end-to-end for `demo-data-engineer` (50 real, on-topic Data Engineer/Scientist candidates). **The generalization rule itself codified** as `HEALTH_LOG.md` review-protocol step 5 (this doc) + pointed to from `CLAUDE.md`'s standing rules, so it survives past this session. | #63 |
| 2026-07-21 | borjan-pm | Working Nomads | **none — manual audit, continuing the sequential walkthrough (7/21).** `raw: 36` looked plausible with no health.py flag; catalog quirk already documented the API as a deliberate, known DEGRADED compromise (2026-07-11: "Category HTML now JS-rendered — API only, skip HTML"). | Re-examined that 2026-07-11 call rather than accepting it as settled: the `exposed_jobs` API is a genuine stub (38 rows total, `?category=` silently ignored/no-op — confirmed live by comparing `category=management` vs `category=project-management`, byte-identical). The category+region page (`/remote-{category}-jobs[-europe]`) IS JS-rendered (a plain `requests` fetch gets 200 but 0 job links — confirmed live), which was the correct part of the 2026-07-11 diagnosis — but headless rendering (used routinely elsewhere in the catalog since then) handles it fine: a headless render captures 50 real, on-topic postings (the site's own AngularJS app makes a POST/ElasticSearch call on load; Playwright's render waits for it, no need to reverse-engineer that API). Also found the catalog's single `-europe`-suffixed URL and the bare (no-suffix) worldwide URL are genuinely SEPARATE listings, not superset/subset — live-checked `project-management`: 50 links on each, only 17 shared — the same non-superset shape as Jobgether's worldwide-vs-emea split. | Switched Working Nomads from the dedicated `fetch_workingnomads()` API handler to the generic `fetch_html_listing` (headless) path: removed the now-dead handler + its `HANDLERS` entry, added `HARVEST_SPECS['working-nomads']` (`/jobs/{slug}`), catalog `fetch_mode: api → headless`. Wired the SAME `region_urls` mechanism built for Jobgether (`profile_loader.region_url_patterns()`, no new code) — worldwide preferred when a profile accepts it, else the existing `-europe` `url_patterns` untouched. **Verified live end-to-end**: borjan-pm (accepts worldwide) resolves to the worldwide URL, raw 36→50, real on-topic candidates present ("project manager blossom" among them); `demo-fe-react` (no `regions_acceptable` set) independently re-checked to confirm its resolved URL is byte-identical to before (still the `-europe` page) — same non-regression check pattern as the Jobgether fix. **Known, documented limitation left as-is rather than over-built**: both the worldwide and `-europe` pages independently cap at ~50 (the SPA's initial-load batch size), so switching to worldwide doesn't recover the ~33 region-only postings that aren't on the chosen region's page — fetching + merging both was considered and explicitly deferred as a future enhancement, proportionate to a single-board fix. Company name isn't cleanly separable from title in the harvested anchor text (concatenated, same class of cosmetic issue as JustRemote's 2026-07-20 finding) — title falls back to a slug-derived one; doesn't affect dedup (URL-keyed) or keyword matching. `core/validate.py` + `tests/run_all.py` (11 targets) green. | PR pending |
| 2026-07-21 | borjan-pm | Arc.dev | **none — Borjan's follow-up, not a health.py flag.** After the Remote Rocketship fix, Borjan pushed back on jumping straight to it: "previous session stopped at Arc.dev (4/21) — let's not trust the old low-yield sweep blindly, given what we just found on Jobgether/Dynamite, double-check for a worldwide/regional split or pagination we missed." | Re-fetched `arc.dev/remote-jobs/project-manager` live: still exactly 6 postings, `?page=2` byte-identical to page 1 (no real pagination), bare `/remote-jobs` (no category) shares 0 URLs with the category page (confirms real category-scoping, not a fluke same-set). The 6-postings verdict itself re-confirmed clean. **But listing every `href` on the page (not just ones matching the existing regex) surfaced a real, previously-invisible bug**: Arc.dev serves postings via TWO parallel URL schemes — native `/remote-jobs/details/{slug}` (what the harvester already caught) and syndicated `/remote-jobs/j/{slug}` (logos point at partner ATS hosts, e.g. a Lever S3 bucket) — the old regex only matched the first. Confirmed the `/j/` set is genuinely category-scoped, not a fixed sitewide widget: the `python` category page showed Data Scientist/Software Engineer `/j/` links, `marketing` showed Growth Marketing Manager/Head of Marketing — different content per category, not noise. Today's PM-category `/j/` batch (Executive Assistant, Content and Social Media Manager, 2x Graphic/Graphics Designer) happens not to keyword-match borjan-pm's PM titles, but the scheme is a live, real coverage gap on every category, not just PM. | `HARVEST_SPECS['arc']` (`core/fetch_boards.py`) widened to `r"/remote-jobs/(?:details\|j)/[\w-]+"`. **Verified live end-to-end** through `fetch_platform()`: raw 6→10 for borjan-pm's PM category (0 of the 4 new candidates keyword-matched this run — expected, not a bug, same as JustRemote's 07-20 finding). **Generalization check, applied to the sibling boards from the SAME 2026-07-20 low-yield sweep** (per Borjan's explicit instruction not to trust that sweep blindly): re-checked Landing.jobs and Crossover for the same "second hidden URL scheme" shape by listing every href, not just regex-matching ones. **Both re-confirmed genuinely clean** — Landing.jobs: exactly 1 real `/at/{company}/{slug}` posting, no other job-shaped href pattern found. Crossover: exactly 1 real `/jobs/{id}/{company}/{slug}` posting among 45 total job-related hrefs, the other 44 all category-tag nav (`/jobs/{category-slug}`, no numeric id) — no second scheme. No other board shares Arc.dev's specific two-scheme HTML structure, so this fix doesn't generalize as a copy-paste change elsewhere — the generalizable lesson is procedural (audit every href shape on a page, not just what the existing regex already expects) rather than a shared code fix. | PR pending |
| 2026-07-21 | borjan-pm | Remote Rocketship (worldwide + Europe) | **none — manual audit, not a health.py flag.** Continuing the standing-rule sweep to boards not yet manually live-checked. `raw: 29`/`23`, `http_ok: true`, no `source_down` — plausible-looking, same blind-spot shape as Jobgether/Dynamite above. | Direct fetch still 403s (the 2026-07-11 Cloudflare quirk), but headless render now reaches real content — no longer a standing degradation. Rendering both category pages live found **zero** matches for the catalog's harvest regex (`/company/{co}/jobs/{slug}`), even though the page visibly contains real postings. Dug into the page's embedded `__NEXT_DATA__` JSON (`initialJobOpenings`) and the raw anchor hrefs: every current posting link is `/publicjobs/company/{co}/jobs/{slug}` — the site added a `/publicjobs/` prefix. **Confirmed this happened mid-day, not a pre-existing bug**: this same morning's scan (`profiles/borjan-pm/state/last_run_candidates.json`) had already harvested the *identical* posting slug (`epi-company-eu/.../test-project-manager-freelance-belgium-remote`) un-prefixed — a same-day re-fetch shows the new `/publicjobs/` prefix on that exact slug, so the regex was genuinely correct this morning and broke later today. | `HARVEST_SPECS['remote-rocketship-worldwide']` and `['remote-rocketship-europe']` (`core/fetch_boards.py`) updated to match the new `/publicjobs/company/{co}/jobs/{slug}` path; `company_idx` bumped 2→3 for the extra path segment. Catalog quirk notes refreshed (stale "DEGRADED" note replaced with the current state + a note to watch for a rollback/mixed-prefix period). **Verified live end-to-end through the real `fetch_platform()` dispatcher**: worldwide 0→25 candidates, Europe 0→23, company/title extraction correct, URLs resolve to the live site. **Generalization check**: spot-checked the other two boards sharing the generic single-page `fetch_html_listing` harvester with a moderate nonzero count (NoDesk 45, no pagination signal found, regex still matches cleanly — genuine, no bug) and Deel's dedicated embedded-JSON careers-page parser (235, unchanged, still parses correctly) — no sibling regression found elsewhere this pass. Remaining un-swept active boards for a future pass: Remote OK, JustJoin.it, Remotive, Working Nomads (all dedicated API handlers, lower selector-drift risk, not yet individually live-diagnosed). | PR pending |
