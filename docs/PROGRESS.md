# PROGRESS — Job Scout Platform build log

> Session-resume contract for the platform build (successor to job-scout-pm's
> BUILD_PROGRESS.md, which stays untouched as the v3 archive). Read this FIRST on any
> session start/resume. Continue from the state below; never redo ✅ steps; update in the
> same session as the work; push before the session ends.

Plan of record: [PROJECT_PLAN.md](PROJECT_PLAN.md) · [ARCHITECTURE.md](ARCHITECTURE.md) · [PROFILE_CONFIG_SPEC.md](PROFILE_CONFIG_SPEC.md) · **Phase 2 build plan: [PHASE_2_PLAN.md](PHASE_2_PLAN.md)** · **Phase 3 build plan: [PHASE_3_PLAN.md](PHASE_3_PLAN.md)**

> **PHASE 2 COMPLETE (steps 2.1–2.12, closed 2026-07-17).** All schema/judgment/catalog/
> template/provisioning/setup/write-back/effort work landed; the **backend-java live test passed**
> (Ani Petreska onboarded, provisioned under Borjan's Notion, 94-candidate scan with an honest
> ledger and correct D7/D8/D20/D22 semantics — see the session log). `borjan-pm` untouched
> throughout (prime directive). Design contract + decisions (D1–D23): **[PHASE_2_PLAN.md](PHASE_2_PLAN.md)**.
> Human-readable guide (2.12): **[PLATFORM_GUIDE.md](PLATFORM_GUIDE.md)**.
>
> **PHASE 3 REFRAMED → APPLICATION COMPANION (2026-07-17).** A vision brainstorm (Opus 4.8) grew
> Phase 3 from an "apply-bot" into the full **Application Companion**: it learns the user's *voice*
> and builds their *knowledge base* (two user-owned assets that grow with every use), then drafts
> applications in-voice, runs interview prep + debrief, keeps the CV sharp, and coaches writing.
> Staged **3a (voice + KB + apply loop — the UAT milestone) → 3b (interview lifecycle) → 3c (CV doctor
> + writing coach)**. A new co-equal **data & privacy principle** was adopted (client-side is the
> destination; Claude is the engine, not the vault; no mining; deletable; strict GDPR) alongside the
> prime directive. Vision + phase scope now live in **[PROJECT_PLAN.md](PROJECT_PLAN.md) §1/§1a/§3**
> (the single source of truth; the roadmap gained Phase 4 = app + client-side store (option b),
> Phase 5 = platform + integrations, and a §3x parked-ideas list). The old apply-bot
> **[PHASE_3_PLAN.md](PHASE_3_PLAN.md)** is **superseded** — its detailed contract is being reworked
> from a focused per-sub-phase planning pass. **The detailed 3a plan is written:
> [PHASE_3A_PLAN.md](PHASE_3A_PLAN.md)** (voice + knowledge base + the apply loop; D1–D16 + checklist
> 3a.0–3a.10). **The 3a.0 Fable 5 review gate RAN 2026-07-17 (@ xhigh): 8 findings + the D13 ⚑
> ruling, all amended into the plan (D1–D16 intact; D13 split confirmed) — outcome note in
> PHASE_3A_PLAN §11.1. NEXT: the Opus 4.8 build (§11.2, steps 3a.1–3a.10)** — after which Borjan
> runs a live scan and UATs 3a.

**Standing production constraint (prime directive): the `borjan-pm` profile is Borjan's live,
daily job search and is sacred. Phase 2+ is purely additive — never regress its behavior, state,
history, or Notion; Borjan keeps using the engine throughout the build; he is account #1 carried in
verbatim when the app ships. See [PHASE_2_PLAN.md §0a](PHASE_2_PLAN.md). (The frozen `job-scout-pm/`
archive also stays intact as the reference/parity anchor.)**

---

## Phase status

| Phase | Deliverable | Status |
|-------|-------------|--------|
| 0 | Documentation set (plan, architecture, config spec, this log) | ✅ merged (PR #8) |
| 1 | Core engine extraction + borjan-pm parity + shortlist liveness sweep | 🟡 built — in review; cutover (1.12) pending Borjan |
| 2 | Template library + setup interview + Notion provisioning | ✅ complete (2.1–2.12; backend-java live test passed; PLATFORM_GUIDE written) |
| 3 | Application Companion (voice + KB + apply/interview/CV/content; staged 3a/3b/3c) | 🟡 reframed & vision-locked (PROJECT_PLAN §1/§1a/§3); detailed 3a plan next |
| 4 | Setup/companion FE app (Agent SDK) | ⬜ not started |

## Phase 0 — Documentation (this PR)

| # | Step | Status | Notes |
|---|------|--------|-------|
| 0.1 | PROJECT_PLAN.md — vision, phases, acceptance criteria, open questions | ✅ 2026-07-13 | 5 open questions for Borjan (plan §5) |
| 0.2 | ARCHITECTURE.md — layers, repo layout, pipeline, Notion contract, sweep design | ✅ 2026-07-13 | |
| 0.3 | PROFILE_CONFIG_SPEC.md — schema draft, option sets, salary model, template format | ✅ 2026-07-13 | Schema is illustrative; normative schema ships in Phase 1 |
| 0.4 | This log seeded with the Phase 1 checklist | ✅ 2026-07-13 | |
| 0.5 | Borjan reviews docs + answers open questions | ✅ 2026-07-13 | Docs merged (PR #8); "lets go" = defaults adopted per plan §5 |

## Phase 1 — Core engine extraction (build checklist)

Ordered so the live scan keeps working at every step. Details: PROJECT_PLAN.md Phase 1,
ARCHITECTURE.md throughout.

| # | Step | Status | Notes |
|---|------|--------|-------|
| 1.1 | `core/schema/profile.schema.yaml` + `profile_loader.py` + `validate.py` (validator in core) | ✅ 2026-07-13 | Strict validation; loader resolves catalog+template+profile → effective config shaped like v3 config.yaml (the parity-friendly design decision) |
| 1.2 | `catalog/platforms.yaml` — extract platform registry, parameterize URLs with `{category}` slots + slug maps | ✅ 2026-07-13 | 23 entries, quirks verbatim; slug-keyed; SE-stream slugs marked `categories_verify_at_setup` (live-verify in Phase 2) |
| 1.3 | `templates/project-management/delivery-manager.yaml` — PM template extracted from SKILL.md + config | ✅ 2026-07-13 | + `software-engineering/frontend-react.yaml` (dry-run only) |
| 1.4 | `profiles/borjan-pm/profile.yaml` — Borjan's values; resolved-config diff vs today's effective config passes | ✅ 2026-07-13 | `core/parity_diff.py` PASSES: key-for-key identical, 16 documented accepted deltas (A1–A7 in the script header) |
| 1.5 | Move scripts → `core/`, thread `--profile` through; state paths profile-namespaced | ✅ 2026-07-13 | + `core/paths.py`, `core/salary.py` (additive `salary_assessment` metadata — never a machine drop) |
| 1.6 | `core/sweep.py` — shortlist liveness sweep + Notion `Stale/Expired` flip | ✅ 2026-07-13 | Seeded-stale acceptance PASSED (retired w/ queued in-place Notion update); 24h deferral verified; live-tested on the 18 real New—Unreviewed rows (all still live, sweep.json committed) |
| 1.7 | `skills/job-scout-run/SKILL.md` — generic run skill (engine policy + judgment layer reads profile) | ✅ 2026-07-13 | v4.0.0, status pre-cutover; three lanes + Appendix A preserved, profile-parameterized |
| 1.8 | Migrate `job-scout-pm/state/` → `profiles/borjan-pm/state/` verbatim | ✅ 2026-07-13 | `core/migrate_state.py`, idempotent union-merge; 765 records/471 URLs migrated. RE-RUN AT CUTOVER — legacy keeps producing state until then |
| 1.9 | Parity runs: full rotation on new engine vs old (coverage ledger, dedup decisions, Notion writes) | ✅ 2026-07-13 | Back-to-back live runs (`--no-headless`): identical covered(14)/down(8) ledgers; all 56 non-LinkedIn candidates IDENTICAL; only diff = LinkedIn tripwire sampling (nondeterministic endpoint). Legacy state git-restored after; Notion writes not exercised (no token) — structural guarantee unchanged + MCP-pending path preserved |
| 1.10 | Demo second profile (dry-run, e.g. fe-react) validates + plans with zero engine changes | ✅ 2026-07-13 | `profiles/demo-fe-react` — `scan.py --plan` produces a coherent React-stream rotation, zero engine changes |
| 1.11 | CI: audit `core/` + catalog + all profiles (replace job-scout-pm target) | ✅ 2026-07-13 | New `validate-platform` job (validate.py + parity_diff.py + per-profile `--plan`); legacy audit KEPT green until 1.12 |
| 1.12 | CUTOVER: re-point laptop + cloud schedules; freeze `job-scout-pm/` as archive | 🟢 freeze complete — only schedule re-point remains | **Full runbook: [CUTOVER.md](CUTOVER.md).** **Legacy freeze DONE 2026-07-14:** first attended run validated the new engine; `job-scout-pm/README.md` archive note (5c); `main` ruleset required check swapped `Validate job-scout-pm` → `Validate platform` (5a); `validate-legacy` CI job + parity step retired, `validate-platform` now the sole CI job/required check (5b). **Only remaining cutover action:** re-point the laptop scheduled task + cloud Routine(s) to the new prompt (`…scan for borjan-pm per skills/job-scout-run/SKILL.md. Unattended mode.`) — Borjan (laptop) + Routine tooling (cloud). Phase 1 closes once schedules fire on the new engine. |

## Phase 2 — Template library + setup interview + Notion provisioning (build checklist)

**Full plan + locked decisions (D1–D23): [PHASE_2_PLAN.md](PHASE_2_PLAN.md).** Ordered so
`borjan-pm` stays production at every step. First real test = onboard a senior Java backend
engineer (post-break, targeting mid/medior part-time) under Borjan's Notion, supervised.

| # | Step | Status | Notes |
|---|------|--------|-------|
| 2.0 | Pre-build review gate (Fable 5, xhigh): scoped adversarial review of the plan; fix blocking findings before 2.1 | ✅ 2026-07-16 | **All 10 findings amended into the plan** — 4 blocking (PR #16: catalog `status`/tier-coverage seam, enforcement wiring assigned to 2.1/2.2, floorless comp contract, `subvariant` marked NEW) + 6 non-blocking in a same-day second pass (`suggestions/` staging dir, `platform_tiers` seeding mechanics, ⭐-template fixture profiles, Notion view-API probe, employment-vocab mapping, `medior` de-banded). D1–D23 intact |
| 2.1 | Schema extensions (`subvariant(+secondary)`, `target_seniority`, `employment_type`, `fte_fraction`, `run.effort(+by_run_type)`, optional floor) + salary.py FTE/floorless wiring | ✅ 2026-07-16 | Additive/non-breaking. `borjan-pm` resolved config **behaviorally identical** (canonical floor 3012.05 unchanged; only additive `fte_fraction: null` metadata key appears) + `--plan` intact. Validator rejects bad enums / `any`-mixing / out-of-range FTE / bad effort; floorless `assess()` stays `unparseable` (never a drop); FTE pro-rates 3012→1506 at 0.5; employment-vocab contradiction → named load warning. Plan §3.1 |
| 2.2 | Template format v2: `platform_tiers` + `salary_estimation_heuristics` (text) + `seniority_titles` + lexicon + run-skill enforcement wiring | ✅ 2026-07-16 | New `core/data/seniority_lexicon.yaml` (D21, medior→mid); loader recognizes v2 template blocks (`platform_tiers`/`seniority_titles`, template-only — `platform_tiers` NEVER loader-merged) + resolves `cfg["seniority_lexicon"]` (base+template). scan.py adds guarded `seniority_detected`/`employment_detected` annotations (fire only when the profile sets the field). SKILL.md v4.2.0: D7 soft/strict seniority + D8 employment accept-set + floorless/pro-rated salary judgment. Both existing templates carry v2 blocks. **borjan-pm behaviorally identical** (only additive `seniority_lexicon`/`fte_fraction` keys). Plan §3.1/§3.3 |
| 2.3 | Catalog expansion: loader `status` rule first; new boards as full `active: false` + `status: unverified` entries; per-stream slug maps; tiers → templates | ✅ 2026-07-16 | Loader `status` rule landed FIRST (`unverified ⇒ active:false`, out of tier-coverage). New-stream category slugs (ai-ml/data/qa/design-ux/marketing/sales/it-support/content-writing/people-hr/devops) added to 6 aggregators (wwr, himalayas, remotive, working-nomads, nodesk, remote-ok), all `categories_verify_at_setup`. 5 new boards (ai-jobs.net, dribbble, behance, problogger, icrunchdata) as `active:false`+`status:unverified`. Tiers already template-owned (2.2 `platform_tiers`). **borjan-pm active rotation bit-identical** (21 platforms unchanged; new boards resolve inactive like europeremotely/otta). Plan §3.2 |
| 2.4 | Template library across the taxonomy: ⭐ tech-core deepest (incl **backend-java**) + a dry-run fixture profile per ⭐ template, business/support/content as groundwork | ✅ 2026-07-16 | 20 new templates (22 total). **backend-java built deepest** (the live-test ⭐). Flagship + dry-run fixture per ⭐ stream: backend-java, ai-engineer, automation-sdet, data-engineer, product-designer (+ frontend-react ✅) — 6 fixtures all `--plan` clean. Every taxonomy stream now has ≥1 template: SE (backend java/node/python, fullstack-js, devops-sre-platform, engineering-manager, frontend-react), ai-ml (ai-engineer, ml-engineer), qa (automation-sdet, manual), data (data-engineer, data-analyst), design-ux (product-designer), product-management (delivery-manager, scrum-master, product-manager), marketing/growth, sales/account-executive, people-hr/recruiter-talent, it-support/technical-support, content-writing/technical-writer. New templates carry no floor numbers (D20). Remaining §2 subvariants = documented backlog (coverage-pending). borjan-pm identical. Plan §2 |
| 2.5 | `core/provision_notion.py`: provision + adopt modes, token/parent params, idempotent; probe view-creation API first (instruct→verify fallback) | ✅ 2026-07-16 | Probed FIRST: parent-page reachable via token; view-creation NOT in REST (`/v1/database_views` 400) → D17 instruct→verify. Built provision/adopt (2025-09-03 data-sources API, extracts data_source_id), idempotent-by-title marker (D19, re-run adopts — verified live), writes output.notion into the profile, lazy select reuse. `core/secrets.py` seam (D18, env fallback). Provisioned Ani's 3 DBs live under Borjan's parent. Plan §5 |
| 2.6 | `skills/job-scout-setup/`: the CV-driven templater/interview skill | ✅ 2026-07-16 | `skills/job-scout-setup/SKILL.md` v1.0.0 — full §4 flow (kickoff→extract→classify+secondary→consent→tailor-in-guardrails→user-chosen posture→provision→persist→validate→first-run→write-back); CV-read-never-stored (D4) + prime-directive callouts. Plan §4 |
| 2.7 | Write-back loop: opt-in consent, generic-only, staged suggestions in `suggestions/` (outside `templates/`), curator merge | ✅ 2026-07-16 | `core/writeback.py`: consent-gated `stage_suggestions` (PII-guarded — refuses anything not a short generic term; upsert w/ provenance + frequency) + `validate_suggestion_file` for CI. New top-level `suggestions/` dir (outside `templates/`) with README (format + curator flow + PII rule). Schema: optional `writeback.consent` (D6, default off). Self-test: kafka/spring-boot/archetype staged, email + sentence refused as PII. borjan-pm identical (writeback absent). Plan §6 |
| 2.8 | Effort/model tier: schema + documented mapping + two-stage design (wiring deferred) | ✅ 2026-07-16 | Schema field landed in 2.1; 2.8 documents the mapping (fast→Haiku, mid→Sonnet, high→Opus), the two-stage judgment design (cheap triage → capable shortlist read via subagent), and the entitlement shape. Run skill v4.3.0 + ARCHITECTURE §7a. Design-and-defer (D10) — recorded/documented only, no model wiring; borjan-pm behavior unchanged. Plan §7 |
| 2.9 | First real test: backend-java onboarding end-to-end; supervised live scan; capture lessons | ✅ 2026-07-16 | **Ani Petreska (Senior Java, targeting mid part-time)** onboarded end-to-end: CV read in-session (never stored), classified → backend-java, profile `ani-backend-java` created, provisioned live under Borjan's Notion, validated, **live scan completed — 94 candidates, 13 covered / 9 down (honest ledger)**. **Semantics behaved:** 44/46 senior+ roles `seniority_off_target` (soft, mid targeted); contract/b2b honored, full-time flagged (D8); 0 salary auto-drops (floorless D20). Lessons captured below. Plan §8, D13 |
| 2.10 | CI: extend `validate-platform` to new schema/templates/catalog + per-⭐-template `--plan` smoke via the 2.4 fixture profiles + `suggestions/` staging check | ✅ 2026-07-16 | Schema/templates/catalog already covered by validate.py; per-⭐ `--plan` smoke covered by ci.yml's `for p in profiles/*` loop (now incl. the 6 ⭐ fixtures); added the `suggestions/**/*.yaml` staging check (`writeback.validate_suggestion_file`) to validate.py. — |
| 2.11 | Docs: update ARCHITECTURE + PROFILE_CONFIG_SPEC + PROGRESS | ✅ 2026-07-16 | ARCHITECTURE repo-layout updated (provision_notion/secrets/writeback/data/suggestions/job-scout-setup) + §7a effort tier; PROFILE_CONFIG_SPEC §2/§3/§5/§6 (all Phase 2 fields, template v2, floorless/FTE, write-back consent) updated across 2.1/2.2/2.7; PROGRESS this table + session log. — |
| 2.12 | Human-readable documentation: `docs/PLATFORM_GUIDE.md` (full project assessment → plain-language guide) | ✅ 2026-07-17 | **New standing rule adopted (PROJECT_PLAN §4): every phase's build plan ends with a human-readable documentation step** — seed it into every Phase 3+ checklist. The guide covers purpose, goal, what Phases 0–2 built, how a scan works, the build method, the Maia worked example (+ Ani as the real proof), repo map, glossary. README now points to it as the entry point. |

## Phase 3 — Application Companion (staged 3a/3b/3c)

**Vision + scope + principles: [PROJECT_PLAN.md](PROJECT_PLAN.md) §1/§1a/§3** (source of truth).
Reframed 2026-07-17 from the old apply-bot plan (now-superseded [PHASE_3_PLAN.md](PHASE_3_PLAN.md)).
Built under both standing principles: the **prime directive** (scanner + `borjan-pm` untouched, Tracker
firewall sacred) and the **data & privacy principle** (client-side destination; Claude is the engine,
not the vault; no mining; deletable). Substrate for Phase 3 = claude.ai Project + Notion, storage
**option (a)** — a knowing, consented interim; client-side store is a Phase 4 deliverable.

**Detailed per-sub-phase execution plans are seeded when each sub-phase is planned** (brainstorm → plan
→ review gate → build) — not pre-planned here (plans go stale). High-level stages:

| Stage | Deliverable | Status |
|-------|-------------|--------|
| 3a | **Voice + knowledge base + the apply loop** — learn the user's voice (docs or guided Q&A; user-judged "sounds like you" meter; voice-only docs shredded after extraction) + stand up the KB + run the per-role loop (re-verify → draft in-voice → capture Q&A → unknown-answer→KB-growth → record). **The UAT milestone: Borjan runs a scan, then tests 3a against real jobs.** | ✅ **COMPLETE 2026-07-19** — build 3a.1–3a.10 done; borjan-pm live loop passed (3a.7); **ani portability passed** (guided-Q&A voice built with no seed + **voice/KB survived a fresh Project conversation** — the save round-trip, confirmed by Borjan 2026-07-19); first-run companion UX gaps fixed + folded into `assistant/` (see 2026-07-19 rows). **One deferred confirmation** (not a gap): companion drafts→creates a NEW Tracker row→next scan back-fills it, unconfirmed only because there were **no open eligible jobs to actually apply to** at UAT time (system correctly steered away from every role) — confirms itself on the first real apply in daily use. |
| 3b | **Interview lifecycle** — on flip-to-Interview: read JD, predict questions, map KB experience onto role gaps; post-interview debrief → honest feedback → KB grows; paste-an-email → flip status. | ⬜ vision-captured |
| 3c | **CV doctor + writing coach** — assess CV, flag if stale (>6mo), guide rewrite, ingest delta doc (Borjan's self-review prompt, §3x); co-write blog posts/articles in-voice → strengthens candidate + feeds voice/KB loop. | ⬜ vision-captured |
| 3.x-docs | **Human-readable documentation** — refresh `docs/PLATFORM_GUIDE.md` at each sub-phase (standing rule, PROJECT_PLAN §4). | ⬜ |

Still-valid reference for 3a's apply loop (settled with Borjan before the reframe, in the superseded
PHASE_3_PLAN): Notion is the only scanner↔companion bridge; write-ownership partitions by row *state*;
the Tracker row is the cross-process dedup handoff; re-verify fetch-first with a paste fallback; a small
PII-free composer binds a profile to its Project. These carry into the 3a detailed plan.

### Phase 3a — build checklist (seeded from [PHASE_3A_PLAN.md](PHASE_3A_PLAN.md) §9)

| # | Step | Status | Notes |
|---|------|--------|-------|
| 3a.0 | Pre-build review gate (Fable 5, xhigh) | ✅ 2026-07-17 | **8 findings + the D13 ⚑ ruling, all amended into the plan** (D1–D16 intact) — full outcome note: PHASE_3A_PLAN §11.1. Key re-scopes: 3a.4 reconciliation is a BUILD (none exists — scan start reads no Notion; `applied` back-fill was chat-flow-only) + a read-before-write sweep guard (blind `apply_sweep_update` PATCH would clobber companion-resolved rows; `User Declined` has no Tracker row to reconcile from); manual voice/KB re-save mechanic made explicit (no programmatic Project-knowledge write) + two-conversation round-trip in 3a.5; select vocabulary pinned; composer snapshot stamp + optional voice-seed; `pitching.md` stays in place; no-PII check defined. D13 ⚑ resolved: split confirmed |
| 3a.1 | `assistant/` generic package (voice, KB, apply loop, write contract, consent language) | ✅ 2026-07-18 | 8 composable modules `00-…07-*.md` (overview, consent/data, voice acquisition, KB+growth loop, apply loop, **pinned** Notion write contract, verification, voice/delivery discipline) + README; generic doctrine extracted from `pitching.md` |
| 3a.2 | Profile-side split — `voice-seed.md` + `data-manifest.md`; `pitching.md` stays intact | ✅ 2026-07-18 | `profiles/borjan-pm/assistant/voice-seed.md` (source-doc priority, Gemini-not-ChatGPT, B2B/visa mechanism, links) + `data-manifest.md`. `references/pitching.md` + `gemini-prompt.md` untouched; run-skill pitch router still resolves pitching.md (verified). `voice-seed.md` optional (ani = guided-Q&A) |
| 3a.3 | `core/compose_assistant.py` (+ compose-date/config-hash stamp; voice-seed optional) | ✅ 2026-07-18 | Deterministic, idempotent (no-op unless body changes), PII-free snapshot (floor/eligibility/location/Notion IDs — **no** display_name/CV facts). `--all` skips dry-run profiles, `--check` gates CI. Handles absent voice-seed (guided path) + absent manifest (generic) |
| 3a.4 | Notion write-contract + reconciliation BUILD + read-before-write sweep guard | ✅ 2026-07-18 | **The ONE permitted scanner change.** `notion_sync.reconcile_applied_from_tracker` (scan-start Tracker READ → seen.jsonl `applied` back-fill; token-gated, idempotent, read-only, sequenced before dedup+sweep in `scan.py`) + read-before-write guard in `apply_sweep_update` (flip only `New — Unreviewed`; no clobber of `User Declined`/`User Applied Elsewhere`). Offline sim proves back-fill/idempotency/guard. borjan-pm byte-identical (`--plan` + resolved-config hash); run skill v4.4.0 |
| 3a.5 | Voice + KB procedure dry-run (incl. two-conversation persistence round-trip) | ✅ 2026-07-18 | Procedures finalized in modules 02/03; `assistant/DRY-RUN.md` = concrete two-conversation script proving the manual re-save round-trip (voice meter + KB growth event → save → fresh conversation retrieves both). No live application (that's 3a.7) |
| 3a.6 | Setup / binding walkthrough (`assistant/SETUP.md` + select-options probe) | ✅ 2026-07-18 | `assistant/SETUP.md`: compose → create Project → paste → upload per manifest → connect Notion MCP → probe-read `New — Unreviewed` → **verify pinned `Reason Passed`/`Status`/`Source` options exist** (cross-checked against `provision_notion` constants) → optional saved-view. Re-compose triggers documented |
| 3a.7 | Acceptance — borjan-pm real loop (dedup + back-fill + sweep no-clobber) + ani portability | ✅ **substantively PASSED 2026-07-18** (supervised live loop w/ Borjan) | **Verified live:** voice built to good-enough + KB grew & **round-tripped across 2 conversations** (file-based, on Claude Desktop/Cowork w/ file access — richer than option-a); binding fixed (bootstrap→custom-instructions + full doc→knowledge, PR #33); apply loop on 2 real roles — **OSF** (prior-history caught a duplicate → flip `User Applied Elsewhere`) + **Britenet** (form-depth re-verify caught Poland-only+German+gambling → flip `User Declined`, no Tracker row); reconcile idempotency **23/0/23** + the 86-row sync **guarded a companion-resolved row live** (no-clobber). **One box not live-tested:** companion drafts + creates a NEW Tracker row → next scan back-fills THAT row (neither real role warranted a new apply — system correctly steered away; mechanism proven via offline sim + OSF's real Tracker + the live 8-back-fill). Confirm on first real apply in daily use. **ani portability: not yet run.** Lessons → tasks #8–#12 (see session log) |
| 3a.8 | CI — composer clean; defined no-PII denylist check; structure lints | ✅ 2026-07-18 | Extended `core/validate.py`: assistant/ structure lints (README/SETUP/DRY-RUN + numbered modules); per-profile binding **compose-clean** (`compose_assistant.compose(check=True)`); binding files pass a **no-PII denylist** (`compose_assistant.pii_hits` — emails/phones/postal, tuned to 0 false positives, teeth-verified); data-manifest non-empty. Runs inside the existing `validate-platform` job |
| 3a.9 | Docs — ARCHITECTURE §5, PROFILE_CONFIG_SPEC, PROGRESS | ✅ 2026-07-18 | ARCHITECTURE §5 (companion pinned transitions, the 3a.4 scan-start reconciliation + read-before-write sweep guard, the composer binding, the known applied-lingering hygiene gap) + repo-layout (`compose_assistant.py`); PROFILE_CONFIG_SPEC §8 (companion binding: bootstrap/instructions split, voice-seed/data-manifest, PII-free composer); this checklist |
| 3a.10 | Human-readable documentation — PLATFORM_GUIDE refresh (standing rule) | ✅ 2026-07-18 | Added the **Phase 3a — the application companion** account to PLATFORM_GUIDE §4 (voice + KB, the Notion bridge / no-collision ownership, the composer/PII-free binding, and Borjan's live-run proof — duplicate catch + form-hidden-disqualifier catch); §4 heading → "Phases 0–3a" |

## Phase 4

Checklist to be seeded when the phase starts (from PROJECT_PLAN.md scope). Do not
pre-plan details here — plans go stale; the plan of record is PROJECT_PLAN.md.

## Session log

| Date | Session | Work done |
|------|---------|-----------|
| 2026-07-20 | Board-coverage audit vs. a 20-board reference list (Sonnet 5, branch `claude/remote-job-boards-coverage-xrg65o`) | Borjan asked whether a reference list of 20 remote job boards was covered. Cross-checked against `catalog/platforms.yaml` + the legacy `job-scout-pm/config.yaml` `rejected_platforms` evidence log: 11/20 active & healthy, 3/20 active but currently degraded (Remotive, Welcome to the Jungle, Remote Rocketship — already tracked in HEALTH_LOG.md), 5/20 deliberately rejected with recorded evidence (Wellfound, Built In, FlexJobs, Jobspresso, Remote.co), 4/20 never evaluated. Live-researched the 4 gaps (WebSearch + WebFetch): **Y Combinator "Work at a Startup"** is a real candidate (anonymous-fetchable, genuine EU/remote PM signal, role+location URL patterns) — seeded as a Phase 2 catalog follow-up. **Dice** (Europe search returns overwhelmingly US staffing postings), **Key Values** (not a postings aggregator — per-company culture pages, no PM category), and **Relocate.me** (no clean PM category, JS-rendered, curated feed paywalled) evaluated and leaning reject, evidence captured. No code changed — findings seeded into [PROJECT_PLAN.md §3x](PROJECT_PLAN.md) so they aren't lost before the Phase 2 catalog work picks them up. |
| 2026-07-20 | Himalayas made adaptive — a fixed 1000-job window left a real coverage gap on long scan gaps (Opus 4.8, branch `claude/health-review-wttj-justremote-2026-07-20`) | Borjan's sharp follow-up ("you crawled all 100k and found 18?") caught something the immediate pagination fix didn't address: by design the fetcher only samples the newest ~1000 jobs (not the full ~100k), which is deliberate (avoid flooding the pipeline), but that window only reaches back **~9.8h** at the site's observed ~100 jobs/hour rate. Checked borjan-pm's real scan history — actual gaps between scans ranged **5-98h** (several 20-33h, one 98.4h multi-day skip) — meaning any gap longer than ~10h left postings **never fetched at all**, a structurally worse problem than undercounting within a window. Checked for a server-side fix first: probed the API for category/query/search filter params (all silently ignored, same pattern as the `limit` bug) and the category-specific HTML page (403s even via headless, no API reference in the tiny response) — no filtering lever available, so the window has to widen. Presented the trade-off (more pages = wider time coverage = more fetch time, ~1s/page) and Borjan chose **adaptive over a fixed constant**: `core/scan.py`'s `run_scan()` now computes the real elapsed hours since the last scan (read-only peek at `runs.json`'s prior `ran_at` timestamps, before this run's own entry is written) and threads it through as `cfg["scan_gap_hours"]`; `core/fetch_boards.py` gained a pure `himalayas_pages_for_gap()` (1.5x safety margin over the observed rate, floor 50 pages/~10h for a fresh profile with no history, ceiling 300 pages/~60h so a multi-day skip can't blow up scan time unbounded) that `fetch_himalayas()` calls instead of a hardcoded page count. Verified live: a 9.8h gap now pulls 74 pages and finds 27 matches (vs. the flat-1000 test's 18). **Unit-tested** (DoD #3 — new behavioral logic in `core/`): `tests/unit_fetch_helpers.py` (7 cases — floor, ceiling, monotonic scaling with gap size, negative-input safety), wired into `tests/run_all.py` (now 6 targets). `validate.py` + `run_all.py` green. No PII. |
| 2026-07-20 | Low-yield sweep — 2 more real bugs found + fixed (Himalayas, Dynamite Jobs); a real gap in the health signals documented (Opus 4.8, branch `claude/health-review-wttj-justremote-2026-07-20`) | Borjan asked the sharp follow-up after the JustRemote fix: could the same "reports success, actually undercounting" shape be hiding on other boards showing a small nonzero count — Arc.dev (6), Himalayas (4), Dynamite Jobs (1), Landing.jobs (1), Crossover (1) — since `health.py`'s signals need a prior higher baseline to detect a "collapse," and a board that's ALWAYS been low has no such baseline to fall from (a real, now-documented blind spot). Manually live-audited all five. **Arc.dev, Landing.jobs, Crossover: genuinely accurate** (checked every href on the page, with/without scroll, plus Crossover's every plausible PM category — no hidden pagination, no missed content). **Himalayas: real bug, fixed.** `?limit=100` is now silently capped at 20/page server-side (verified: limit 20/50/100/200 all return exactly 20); the old `offset={page*100}` loop assumed 100/page, so it skipped 80 real jobs between every request — a sparse, gap-filled sample. Fixed to `offset={page*20}` over 50 pages (still newest-1000 total). Verified live: **18 keyword matches vs. the buggy 4 (4.5x)**, including much more clearly on-target titles. Affects every profile using Himalayas (Tier 2), not just PM. **Dynamite Jobs: real bug, fixed.** Most postings render as `<h2 href="...">`, not a real `<a>` tag — `_harvest_links()`'s `tree.css("a")` selector structurally couldn't see them (15/16 real postings invisible). Widened to `tree.css("[href]")` (any href-bearing element; the existing per-platform regex already filters out any noise this picks up). Verified live: **16/16 recovered vs. the buggy 1.** This is a shared-function fix, not board-specific — benefits any board with this pattern. **Documented the underlying gap** in `docs/HEALTH_MONITORING.md` ("Known blind spot") and seeded a future signal idea (flag a Tier 1/2 board whose yield has never once exceeded a floor, independent of trend) into `docs/PROJECT_PLAN.md`'s parked table per DoD #6 — not built, since the right floor needs real per-platform volume data this build doesn't have yet. Full audit table + both fixes recorded in `docs/HEALTH_LOG.md`. `validate.py` + `run_all.py` (5 targets) green. No PII. |
| 2026-07-20 | JustRemote actually fixed — the lazy-load diagnosis was wrong (Opus 4.8, branch `claude/health-review-wttj-justremote-2026-07-20`) | Borjan asked to check `https://justremote.co/remote-jobs` directly, which the prior two diagnosis rounds (the pre-existing 2026-07-11 quirk re-confirmation, and the `render.py` `scroll_rounds` test) hadn't tried. That surfaced the real root cause: the site dropped its "project-management" nav category (successor is `manager-exec`), and postings now render under a **relative href with no leading slash** (`remote-manager-exec-jobs/product-owner-...`) instead of the old absolute `/remote-jobs/{category}/{slug}` — the `HARVEST_SPEC` regex never matched the new shape, so every prior audit (07-11, 07-14, and this session's own scroll test) silently harvested 0 regardless of whether real content existed. Not a lazy-load problem at all — plain headless (no scroll) already renders the postings. **Two-part catalog fix**: `catalog/platforms.yaml`'s `categories.project-management` updated `project-management` → `manager-exec`; `core/fetch_boards.py`'s `HARVEST_SPECS['justremote']` regex updated to match the relative href shape (`min_hyphens: 2` to filter nav noise). **Verified live end-to-end**: `raw: 17, source_down: False, http_ok: True` — a real board back from the dead, including genuinely PM-adjacent postings (Principal Product Manager @ Gitlab, Product Owner @ Nutrafol). 0/17 keyword-matched borjan-pm's specific title set this run — confirmed expected (small mixed-bag category, not every run has a PM-titled posting) rather than a filter bug, by independently checking substring matching still works correctly on the (cosmetically) concatenated titles. **Known minor issue left as a documented quirk, not silently fixed**: company + title concatenate without a separator (two adjacent DOM elements, no whitespace text node between them) — doesn't affect dedup or matching, just display polish; flagged for a future bespoke parser if ever prioritized. This supersedes the "no fix available" conclusion from the same session's earlier round — a good reminder that a live board-rot diagnosis isn't done until you've actually checked the site's own top-level pages, not just the URL already in the catalog. `validate.py` + `run_all.py` (5 targets) green. Recorded in `docs/HEALTH_LOG.md`. No PII. |
| 2026-07-20 | Scoped `render.py` policy amendment — scroll-only exception, tested against JustRemote (Opus 4.8, branch `claude/health-review-wttj-justremote-2026-07-20`) | Follow-up to the live diagnosis above. `render.py`'s module docstring is a deliberate, structural ban ("the entire public API surface is `render(url) -> html`... do not extend," Borjan 2026-07-11) specifically to keep computer-use-style interaction out of the fetch layer. Borjan reviewed the exact mechanics (confirmed: no screenshot/image/vision anywhere — just `page.evaluate("window.scrollTo(...)")`, a passive viewport move, still returning HTML text for the existing parser) and **approved a narrow, scoped exception**: a new `scroll_rounds` parameter on `render()`/`render_many()` that scrolls to the page bottom N times before snapshotting, for boards whose listings lazy-load. Explicitly still bans click/type/navigation-decision — the ban stays structural for everything except this one passive primitive. Wired into `fetch_boards.py` as a new `fetch_mode: headless_scroll` (dispatched in `fetch_platform`, threaded through `fetch_html_listing`'s `scroll_rounds` param into its `render.render()` call). **Tested live against JustRemote** (the motivating case) with both `window.scrollTo` and a real `page.mouse.wheel` event, several rounds each: content size grew slightly but **zero real postings surfaced either way** — so JustRemote's catalog entry was deliberately **left on `fetch_mode: headless` (not switched to `headless_scroll`)**, since the mode wouldn't fix this board and would only add latency; quirk note updated with the full test history instead. The capability itself stays built and available for a future board where scroll genuinely is the blocker. `validate.py` + `run_all.py` (5 targets) green — fetch/render I/O stays outside unit-test scope per the repo's testing doctrine (honest-failure + health-monitoring covers it), consistent with existing coverage decisions. Recorded in `docs/HEALTH_LOG.md`. No PII. |
| 2026-07-20 | Live-diagnosed the two open health findings — JustRemote + Welcome to the Jungle (Opus 4.8, branch `claude/health-review-wttj-justremote-2026-07-20`) | Follow-up to the first real review (PR #54): fetched both boards live to resolve the open `NEVER_PRODUCED` items. **JustRemote** — fetched the profile's category URL (direct + headless) and, suspecting a renamed category (its nav no longer lists "project-management," only `manager-exec`), fetched that renamed page too; **both return 0 postings** (just blog/nav links). Root cause is the catalog's *pre-existing* 2026-07-11 quirk note (lazy-load-on-scroll, no API channel) — confirmed still accurate; the rename theory doesn't hold since the renamed page has the identical blocker. **No catalog change** — this validates `health.py` correctly re-flagged a real, already-known-unresolved issue rather than a false signal. **Welcome to the Jungle** — genuinely new: fetched direct + headless for both the profile's filtered query URL and the bare `/jobs` listing (no filters at all); all four returned only the marketing/app shell — 0 job/company links, no `__NEXT_DATA__`, no embedded Algolia app-id despite Algolia-InstantSearch-shaped URL params. **Added a quirk note to `catalog/platforms.yaml`** documenting this (fetch behavior unchanged — a snapshot fetch genuinely can't reach the content right now regardless of mode); flagged as needing a longer-interaction headless pass or their internal search API reverse-engineered, left for a future session rather than guessed at. Both findings recorded in `docs/HEALTH_LOG.md`'s Review log with full evidence. `validate.py` green. No PII. |
| 2026-07-20 | First real Platform Health review — borjan-pm residential scan (Sonnet 5, local session) | The first genuine daily scan since PR #52/#53 merged (residential IP, not the earlier cloud-IP QA test): 114 new candidates, 20 platforms covered, 3 shortlisted to Notion `New — Unreviewed` (Link Group PM/SM, BPX S.A. Business Connectivity PM, Craftware Scrum Master — all fully remote, no eligibility risk), 61 judgment-dropped (mostly Poland hybrid/on-site + non-target-language + travel clauses), 50 logged unverified/blocked (LinkedIn tripwire auth-wall + full-read-cap misses). **`core/health.py --profile borjan-pm` ran clean on real telemetry**: 6 `NEVER_PRODUCED` findings, zero `SELECTOR_SUSPECT`/`DOWN_STREAK`/`YIELD_COLLAPSE`/`SYSTEMIC`. Four are the known config gap (empty `ats_boards` for Greenhouse/Lever/Workable/Pinpoint — not a fault). The other two — **Welcome to the Jungle + JustRemote — reclassified**: previously read as blocked outages (`sources_down`/would-be `DOWN_STREAK`), now show `http_ok: true, raw: 0` on real residential telemetry, i.e. genuinely reached-but-empty. Left **open** for a live-fetch diagnosis (selector drift vs. bot-wall shell page) — recorded in `docs/HEALTH_LOG.md`'s Review log as the first real entries, superseding the "no live review yet" placeholder. Self-healing: none fired this run (every reachable board answered cleanly first try from residential IP). Both gates: `validate.py` green; `run_all.py` not re-run (no `core/` behavioral change this session). No PII. |
| 2026-07-20 | Live QA of Platform Health build — end-to-end proof + a false-heal fix (Opus 4.8, branch `claude/platform-health-self-healing-jth8o1`) | Ran a full live `scan.py --profile borjan-pm` from the build container (cloud IP) to prove the health telemetry pipeline end-to-end: 21 boards fetched, 108 new candidates, `runs.json` correctly wrote the new `platform_stats` (`raw`/`source_down`/`http_ok`) + `healed` fields, and `core/health.py` read them and flagged `NEVER_PRODUCED×8` (Greenhouse/Lever/Workable/Pinpoint ATS boards with empty token lists, plus JustRemote/WTTJ/Remote Rocketship×2 — all reached but zero, correctly NOT read as connectivity failures anymore). **QA finding #1:** JustRemote and WTTJ answered a plain HTTP 200 with 0 harvested — previously indistinguishable from a blocked outage, now visibly the reached-but-empty shape (recorded in `docs/HEALTH_LOG.md` for the first real review to confirm selector drift vs. bot-wall). **QA finding #2 (fixed same session):** Remote Rocketship escalated direct→headless after a 403, got a body back, but still harvested 0 — yet the original code reported it as `✚ healed[…]: recovered via headless`, overstating a mere reachability proof as a recovery. **Fix:** `fetch_html_listing` now only claims a heal when the escalation *also* recovers ≥1 candidate; otherwise logs `escalated to headless for …, still 0 harvested` — an honest non-recovery, matching the honest-failure floor (never a paper-over). Verified live against Remote Rocketship post-fix: `healed=[]`, correct honest note. **Production state fully restored** after the test scan (cloud-IP telemetry doesn't belong in borjan-pm's real baseline — backed up pre-scan, diffed clean after restore). Also locked in Borjan's threshold decisions: all seven Layer-1 thresholds are **system-wide + immutable** (no user/profile override) — `window 6`, `down_streak 4`, `yield_collapse_factor 0.15`, `min_baseline 3`, `never_produced_min_runs 4`, `systemic_frac 0.7`, `due_at_sessions 6` — and seeded the user-facing counterpart as **Phase 4 app scope**: a Platform settings screen exposing per-board analytics + live/off + descope reason, with user re-enable of a descoped board (restarts its health count) since a board may show zero simply because there were no openings then, not because it broke. Documented in `HEALTH_MONITORING.md` ("In-app platform settings"), `PROJECT_PLAN.md` §4 item 7, `PHASE_3_HEALTH_PLAN.md`. `validate.py` + `run_all.py` (5 targets) green throughout. No PII. |
| 2026-07-20 | Health thresholds locked (immutable) + app platform-settings screen seeded (Opus 4.8, branch `claude/platform-health-self-healing-jth8o1`) | Borjan's calls after reviewing the seven Layer-1 knobs: **all thresholds are system-wide + IMMUTABLE — not user-configurable, not profile-overridable** (detection sensitivity is a platform-engineering decision; a user knob there could hide real rot or drown a user in false alarms). Set the values: `window 8→6`, `down_streak 3→4`, `yield_collapse_factor 0.25→0.15`, `min_baseline 6→3`, `never_produced_min_runs 4`, `systemic_frac 0.7`, `due_at_sessions 6` — in `core/defaults.yaml` (with the immutability rationale) + the mirrored `_DEFAULT_TH` fallback in `core/health.py`. **Seeded the user-facing half as Phase 4 app scope (DoD #6):** an in-app **Platform settings screen** exposing the *outcome* layer (never the knobs) — per-board analytics (scans/yields/live-off/last-produced), **notify the user when the health check descopes a board** (dead/unrecoverable) with the plain reason, and **let the user re-enable a descoped board anyway** (it may have yielded nothing simply because there were no openings then, not because it's broken), which **restarts that board's health count**. Precedence mirrors Layer-2-runtime (user override supersedes auto-descope for their instance; a later shipped fix expires the override; descope is per-user instance state, never a repo catalog edit). Documented in `HEALTH_MONITORING.md` ("In-app platform settings"), `PROJECT_PLAN.md` §4 item 7, and `PHASE_3_HEALTH_PLAN.md` (Deferred). The engine-side "restart a board's health count" primitive is parked until auto-descope (Phase 4) creates something to restart. `validate.py` + `run_all.py` (5 targets, incl. unit_health) green; unit thresholds are self-contained so the default change doesn't shift them. No engine behavior change beyond the new threshold values; borjan-pm resolved config carries only the new immutable `health:` values. No PII. |
| 2026-07-20 | Platform Health & Self-Healing — Layers 1 + 1.5 + the Layer-2 cue (Opus 4.8, branch `claude/platform-health-self-healing-jth8o1`) | Built the roadmap-sequenced health interstitial (between 3a and 3b) from the [HEALTH_MONITORING.md](HEALTH_MONITORING.md) seed — build record in **[PHASE_3_HEALTH_PLAN.md](PHASE_3_HEALTH_PLAN.md)**. **The gap it closes:** the *silent selector break* — a board returns HTTP 200 with HTML but the parser extracts 0 rows (markup changed), so `source_down` stays false and it hides. **Telemetry prerequisite:** `core/scan.py` now records a per-run `platform_stats` map in `runs.json` (`{platform → {raw, source_down, http_ok}}`); the new `http_ok` axis (*did the board actually answer?*) is what separates a silent selector break (`source_down` true, `http_ok` true = reached-200-parsed-0) from a plain outage (`http_ok` false). `http_ok` threads through `fetch_boards.py`'s `_ok`/`_down` helpers; the reached-but-empty downs (WWR "RSS parsed to 0", Deel "careers JSON yielded 0", `fetch_html_listing` 200-but-0-harvested) are marked `http_ok=True`. **Layer 1** — new `core/health.py`: pure `compute_health(runs, active_names, thresholds)` over the telemetry (no I/O, no network, no judgment) emitting five severity-flagged signals — `SELECTOR_SUSPECT` (the silent break), `DOWN_STREAK` (hard-down N in a row), `YIELD_COLLAPSE` (raw far below trailing median), `NEVER_PRODUCED` (active + reached but 0 ever), `SYSTEMIC` (most active boards ~0 at once → core/network fault; suppresses the per-board symptom flags). Thresholds in `core/defaults.yaml` `health:`, resolved per profile. CLI prints a human summary or `--json`; legacy runs still yield DOWN_STREAK from the old `sources_down` list. **Layer 1.5** — in-scan self-healing in `fetch_boards.py`: `_get` retries with bounded backoff (2s, 4s) on *transient* failures only (connection resets, timeouts, 429/5xx — never a real 4xx), and `fetch_html_listing` escalates direct→headless when blocked; every recovery is **always reported** (`healed` list → `runs.json` + ledger `✚ healed[board]: …`), never a silent paper-over (honest-failure floor preserved). **The cue** — a `health_review_due` counter rides the recompute mechanism: `scan.py` prints `⚠ platform health review due` every `health.due_at_sessions` sessions; running `core/health.py` acks it. **Layer 2** wired into the `job-scout-run` skill (→ **4.8.0**): on the cue, run `health.py`, diagnose each flagged board live, repair through the catalog + validator (never an ad-hoc scanner edit; prime directive holds). **Tests (DoD #3):** new `tests/unit_health.py` (13 cases — every signal fires on its shape + stays quiet otherwise, incl. legacy telemetry + empty) wired into `tests/run_all.py`. **Layer 2-runtime deferred to Phase 4+** (needs the embedded LLM). Verified on real telemetry (borjan-pm DOWN_STREAK×2 for JustRemote + WTTJ; ani DOWN_STREAK×9). `validate.py` + `run_all.py` (5 targets) green. **Additive — `borjan-pm` resolved config + what the scan resolves/scans/writes unchanged** (new fields are additional telemetry + an additional ledger line). No PII. |
| 2026-07-20 | Dedup-key fixes — norm_url slash-before-query + norm_company Polish `Sp. z o.o.` (Opus 4.8, branch `claude/whats-next-f2h4d1`) | Fixed the two dedup quirks the behavioral-test gate pinned as characterization tests the same day (the "deliberate fix" they were left for). **(1) `norm_url`:** `.rstrip("/")` ran BEFORE the query was stripped, so a slash sitting in front of `?`/`#` survived (`/job/?utm=x` → `.../job/` ≠ `.../job`); reordered to strip the query/fragment first, then the trailing slash — `/job/?x` now keys identically to `/job?x` and `/job`. **(2) `norm_company`:** added the Polish legal-form family to `LEGAL_SUFFIXES` — `sp\.?\s*z\s*o\.?\s*o\.?` (Sp. z o.o. = LLC, incl. the combined `Sp. z o.o. Sp. k.` via the stacked-strip loop) + `sp\.?\s*[kj]\.?` (Sp. k./Sp. j.), so `Finture Sp. z o.o.` now key-matches a bare `Finture` (JustJoin.it varies the suffix). **Tests (DoD #3):** flipped both characterization tests in `tests/unit_dedup.py` to assert the corrected behavior + added identity assertions (`/job/?x`≡`/job?x`; `Finture Sp. z o.o.`≡`Finture`; combined `Sp. z o.o. Sp. k.`→`acme`). **Blast radius measured over borjan-pm's real `seen.jsonl` (1315 records):** exactly 59 company-key changes (all Polish `Sp. z o.o.` variants collapsing to the bare name — incl. no-dot `Sp.z o o`, spaced `Sp. z o. o.`, combined `Sp. z o.o. Sp. j.`) + 2 URL-key changes (both Deel `careers/position/?ashby_jid=…`, already query-collapsed under the old code — the fix only drops the leftover trailing slash, no new merge). Strictly more-correct dedup; the 3-part key still needs role+locdom to match, so no distinct companies over-merge. **borjan-pm resolved config untouched** (runtime dedup helpers, not config). `tests/run_all.py` (4 targets) + `validate-platform` green. No PII. |
| 2026-07-20 | Behavioral test gate — starter unit suite + sims in CI + scoped DoD clause (Opus 4.8, branch `claude/job-scan-results-bnn5y6`) | Borjan asked about test-coverage status + whether to make it a DoD discipline. **Assessment:** the only automated coverage was `validate.py` (structural — proves files COMPILE + configs RESOLVE, never that a detector returns the right answer) + `--plan` smoke; the two `sims/` weren't even in CI; and the pure-logic layer (scan.py detectors, dedup helpers) — exactly where this session's leaks lived — was re-proven inline each session and thrown away. **Built** (matching the repo's no-pytest self-asserting convention): `tests/_harness.py` (`Suite`), `tests/unit_detectors.py` (26 cases over detect_language/language_flag/stated_language_requirement/detect_employment/detect_work_arrangement/start_date_passed/detect_seniority), `tests/unit_dedup.py` (norm_url/norm_company/role_family), `tests/run_all.py` (runs unit suites + both sims as isolated subprocesses; CI entry), `tests/README.md`. **Wired into CI** as a new "Behavioral tests" step in `validate-platform` (no network/token — sims mock the boundary; `import scan` verified clean under CI's pyyaml+requests only). **DoD #3 extended** (CLAUDE.md): `tests/run_all.py` green + a scoped rule — new/changed behavioral logic in `core/` ships a committed unit test (pure fns) or sim (boundaries) in the same PR; fetch/render I/O stays on honest-failure+health-monitoring, not unit tests. **Writing the suite immediately paid off — surfaced two latent dedup quirks** (pinned as characterization tests): `norm_url` strips a trailing slash BEFORE the query, so `/job/?utm=x` ≠ `/job`; and `norm_company` doesn't strip the Polish `Sp. z o.o.` suffix, so `Finture Sp. z o.o.` won't key-match a bare `Finture`. Both are dedup-key behavioral changes → left for a deliberate fix + borjan-pm byte-identical proof, not slipped into this PR. `validate` + `run_all` (4 targets) green. No PII. |
| 2026-07-20 | Twin-row dedup fix — idempotent Passed/Seen sync + reconcile reason-ranking (Opus 4.8, branch `claude/job-scan-results-bnn5y6`) | Follow-up to the row below: fixed the duplicate-Passed/Seen-row bug that surfaced on Vazco + TT MS (a mechanical `Filtered Out` twin next to the user's `User Declined`, one URL). **Root cause:** a posting resolved on an earlier scan resurfaced as a fresh unsynced record, and `sync_scan`'s CREATE path blindly `typed_create_page`d a SECOND Passed/Seen row (created_time proved the 07-19 Filtered-Out + 07-20 User-Declined split); `reconcile_resolutions_from_passed_seen` then applied both in Notion row-ORDER (last-wins), making the local resolution nondeterministic. **Fix (two halves):** (a) CREATE path is now idempotent-by-URL — `find_page_by_url` first; a resolved row is left untouched (no clobber, no dup), a still-live row is PATCHed in place, and a create fires only when no row exists (tokenless MCP instructions updated to match); (b) reconcile collapses rows per URL and ranks reasons via `_REASON_PRIORITY` (User Applied Elsewhere > User Declined > mechanical Filtered/Duplicate/Stale > live), so even pre-existing twins resolve deterministically and a user decision is never overwritten by a machine drop. **Proven offline** by new `sims/twin_row_dedup.py` (both Notion row orders → User Declined wins; create fires once for a brand-new URL, never for a URL that already has a row; resolved row untouched, live row refreshed). `validate-platform` green; existing `reconcile_new_tracker_box` sim still passes; borjan-pm config unaffected. No PII. |
| 2026-07-20 | Stated-language-requirement flag + borjan-pm 5-decline reconcile (Opus 4.8, branch `claude/job-scan-results-bnn5y6`) | Borjan reviewed his latest scan (5 open New — Unverified) and declined all 5, 4 citing Polish. **Built** `stated_language_requirement` in `core/scan.py` (flag mode, per his call): fires when a JD written IN a target language (English) STATES a hard proficiency requirement in a NON-target language ("Polish C1", "fluent Polish", "apply in Polish") — distinct from `non_target_language` (which reads the JD's OWN dominant language, so it reads these English JDs as `en` and never fires). Conservative: nice-to-have mentions never fire; excludes generic level words (b2/advanced) that collide with "B2B"/boilerplate; `_LANG_NAMES` covers 27 EU languages. 9 synthetic cases + all 5 real JDs pass. **Root-cause finding (bigger than the detector):** the scan's cached `jd_text` for these JustJoin.it roles was 1500 chars of raw CSS (21 words) — that scan ran in a `selectolax`-degraded session, so NO body-reading flag (mine or `non_target_language`) could fire. Re-fetched with selectolax present → clean 9.5k-char bodies; the detector then correctly flags Vazco (Polish B2+/C1), Finture (Polish C1), TT MS (Polish C1) and correctly stays SILENT on **Addepto** — whose real JD requires only Fluent English C1/C2, no Polish. Addepto's Polish-decline reason was made on absent data; the real Polish signal was a question in the application FORM (unscannable), EU eligibility is the primary reason (Borjan confirmed) — corrected its Notion note. **Declines recorded:** all 5 were already `User Declined` in Notion; local `seen.jsonl` had drifted (`shortlisted`) — ran `reconcile_resolutions_from_passed_seen` (canonical #13 path, resolved 7 incl. 2 other drifted rows). Vazco + TT MS carry a duplicate `Filtered Out` dedup-twin Notion row (last-wins flipped them to dropped) — set both to `User Declined` locally to reflect Borjan's actual decision. **Seeded** (DoD #6): the duplicate-Passed/Seen-twin-row bug + the apply-form-language residual gap → PROJECT_PLAN §3x; coverage row + skill flag doc updated. `validate-platform` green; borjan-pm config unaffected (a flag is additive annotation, not config). No PII. |
| 2026-07-20 | Dependency install hardening: requirements.txt + SessionStart hook (Sonnet 5, branch `claude/ani-project-setup-2rb1b0`) | Borjan asked whether `selectolax` (installed manually to fix the JD-extraction bug) would persist across sessions — it wouldn't; this is an ephemeral cloud container, and there was no `requirements.txt` or session-start automation, only prose (`LAPTOP_LANE.md`, the run skill's setup line). Any fresh session/clone was one `pip install` away from repeating the exact silent-degradation bug. **Fix:** added `requirements.txt` (requests/pyyaml/selectolax/playwright, matching the documented set) + a `SessionStart` hook (`.claude/hooks/session-start.sh`, registered in `.claude/settings.json` alongside the existing Stop hook) that installs it automatically every session — remote/managed environments skip `playwright install chromium` (pre-installed, `PLAYWRIGHT_BROWSERS_PATH` already set); local/laptop-lane installs it once, idempotently. Validated: uninstalled selectolax, ran the hook in remote mode, confirmed all 4 packages resolve; ran twice for idempotency; confirmed the non-remote branch correctly skips the browser download when the path already exists; `validate-platform` + a `--plan` scan pass green. Docs (`LAPTOP_LANE.md`, the run skill's setup line) now point at `requirements.txt` as the source of truth instead of a hand-typed line that can drift. CI's install step intentionally untouched — it never fetches JDs (network-free `--plan` only), so `selectolax`/`playwright` add nothing there. Skill → **4.6.1**. Answered directly: the eventual **app** (Phase 4) is unaffected by this class of risk — a packaged app pins dependencies at build time via a lockfile/bundle, not per-session `pip install`; the exposure is scoped to code-lane sessions between now and Phase 4, which this hook closes. |
| 2026-07-20 | Fixed the JD-extraction root cause (Sonnet 5, branch `claude/ani-project-setup-2rb1b0`) | Diagnosed and fixed the finding from the overnight judgment pass (below): `core/fetch_boards.py`'s `_visible_text()` has a proper `selectolax` DOM-parse path that strips `<script>`/`<style>` content correctly, but silently falls back to a naive regex tag-stripper on any exception — and that fallback only removes tag markers, not `<style>`/`<script>` block CONTENT, so raw CSS/JS text leaks into "JD text." `selectolax` wasn't installed in the session that ran the overnight scan, so every JustJoin.it fetch silently took the degraded path — explaining the 60%/47% Polish-miss rate found that night. **Fix:** the regex fallback now strips script/style block content first (`_SCRIPT_STYLE_BLOCK`), and prints a stderr warning when it's used, so a missing/broken selectolax degrades honestly instead of corrupting every downstream text detector silently. `selectolax` installed in-session. Unit-verified both paths (selectolax present/absent) produce identical clean text. `validate-platform` green. That night's already-synced Notion shortlists were hand-corrected via manual reads and don't need a rewrite — this fix protects every future run. |
| 2026-07-20 | Overnight unattended judgment pass + Notion write + JD-extraction finding (Sonnet 5, branch `claude/ani-project-setup-2rb1b0`) | Completed the judgment-layer pass Borjan asked for on the previous night's captured scans and wrote both shortlists to Notion. **Pre-judgment bug caught live:** the work-arrangement detector missed JustJoin.it's `(office)` loc-tag synonym (only `on-site`/`onsite`/`stationary` were covered) — 6 on-site Ani candidates had leaked past the remote-only drop; fixed (`_LOC_ARR_TAG` + synonym map) and re-applied to the already-captured data (own commit, `af3f476`→recovered as `b1b97a0` after a branch-restart mishap since PR #47 had already merged). **Bigger finding during manual JD reads:** JustJoin.it's fetched JD text is ~700KB of CSS/framework boilerplate; the scanned/cached 1500-char `jd_text` field never reaches the real content (~240k chars in) — silently starving `language_flag()` of real signal. Confirmed empirically: **15/25 (60%) of Ani's and 9/19 (47%) of borjan-pm's fully-fetched JJIT candidates were actually Polish**, undetected by the mechanical flag. Not fixed tonight (fetch/extraction-layer change, too large to rush unattended) — worked around by manually reading full cached JDs and hand-dropping the confirmed-Polish ones; seeded as a high-severity next-session item in [ENFORCEMENT_COVERAGE.md](ENFORCEMENT_COVERAGE.md). **Judgment pass results:** Ani — 10 shortlisted (Jit Team, RemoDevs, Antal, emagine×2, Spyrosoft, Andersen, Netguru×2, OrderYOYO; scores 7.0-8.3) + 178 total drops (168 mechanical + 6 office-tag + 16 Polish-reread + 4 domain/location/role judgment: Caspian One financial-domain-required, Xello Canada-only, Speechify iOS-not-Java, Rezdy below-threshold). borjan-pm — 5 shortlisted (Transition Technologies MS, Vazco, Addepto, Finture, Andersen; scores 7.3-8.0) + 68 judgment drops (46 hybrid/on-site — work_model=remote enforced in judgment since he's flag-mode, not opted into the mechanical toggle; 9 Polish; 5 US/UK-tagged eligibility-unconfirmed; 4 role-mismatch/timezone/junior-level, incl. 2 "Infrastructure Program Manager" postings that were actually hands-on cloud-engineering roles mislabeled). **Notion sync:** both profiles fully synced (Ani 67 pushed/0 failed; borjan 67 pushed/2 failed — 2 ERGO Technology rows hit a post-write title-assertion mismatch from zero-width unicode chars in the scraped title, both drop-reason rows, non-critical, left unsynced). Both profiles' state pushed. `validate-platform` green throughout; borjan-pm resolved-config still byte-identical (only Ani's `hard_filters` + both `seen.jsonl`/`last_run_candidates.json` state changed). No PII. |
| 2026-07-19 | Session wrap + Phase 3a close + seed-for-later DoD (Opus 4.8, branch `claude/ani-project-setup-2rb1b0`) | Wrap-up of the Ani first-run session. **(1) Phase 3a marked COMPLETE** — ani portability passed (guided-Q&A voice with no seed + voice/KB survived a fresh Project conversation, the save round-trip, confirmed by Borjan); the only deferred item (companion-creates-NEW-Tracker-row → next-scan back-fill) is blocked solely by *no open eligible jobs to apply to*, not a system gap — self-confirms on first daily apply. **(2) Proven-prompt fold** (DoD #1 / Stop hook): tuned `GUIDED-FLOW.md` D1 to the dogfooded voice-build prompt (guided-Q&A with pickable example answers, tone/gender + English-level/complexity dials, numbered skippable phases, two samples per phase, save-by-default). **(3) New DoD #6 in CLAUDE.md — "seed unbuildable ideas into the plan of record"**: any session feedback/idea that can't be built now must be seeded into PROJECT_PLAN (phase scope or §3x parked) before wrapping, so nothing dies in chat/side-docs. **(4) Seeded everything outstanding:** §3 app-UX → PROJECT_PLAN Phase 4 §6; and §3x parked rows for `target_seniority.strict` mechanical drop, library-grade language detection, over-constraint nudge → app UI + configurable threshold, and the Ani app-UX bundle. `validate-platform` green. Then ran an **unattended scan** (results for Borjan to read next day). No PII. |
| 2026-07-19 | Companion doctrine fold — voice/apply UX from Ani's live run (Opus 4.8, branch `claude/ani-project-setup-2rb1b0`) | Folded the §2 proven-by-failure companion lessons into the generic `assistant/` modules (inherited by every profile), then recomposed both bindings. **`02-voice-acquisition.md`:** never ask a bare open question — every guided-Q&A question ships **selectable options + worked examples** (assistant proposes 2–4 pickable answers + free-write escape; the "quirks" question now carries concrete examples); added an explicit **tone/gender dial** (a first draft with none reads generic — the user had to ask for "feminine touches") and **sentence-complexity + English-level dials**; calibration is now **numbered, skippable phases** ("voice generation 1/N", N estimated from input richness: rich uploads→0, one doc→~4, thin Q&A→5–6) with **two candidate samples per phase** to compare, skippable with an honest "voice may not be thoroughly set" warning; **save-by-default** stated up front (not an opt-in question). **`03-knowledge-base.md`:** same save-by-default framing. **`04-apply-loop.md`:** **always emit the posting's clickable link**; "how to proceed" as **explicit pickable options** (Apply/Pass/Dead/Skip) each mapping to a pinned Notion status — the tap is the record. Bindings recomposed (config-hash unchanged — prose-only, snapshot untouched); `compose --all --check` clean; `validate-platform` green. Feedback doc §2 marked ✅. No engine change; no PII. |
| 2026-07-19 | Enforcement generalization: language-set + drop telemetry + audit (Opus 4.8, branch `claude/ani-project-setup-2rb1b0`) | Borjan's follow-up to the leak fix: enforce ALL declared params (not just the two noticed), guard against over-constraining, and generalize the language rule. **(1) Language generalized** — `core/scan.py` replaces the hardcoded EN-vs-PL check with `detect_language()` over a known stopword set (en/pl/de/es/fr/it/nl/pt + diacritic boosters) and `language_flag(text, target_langs)`; new `search.languages` config (default `[en]`, multi-value e.g. `[de,en]` keeps both), flag renamed `non_english_jd`→`non_target_language`, title-level diacritic fallback for JD-less twins. New opt-in `hard_filters.language_mismatch: [flag|drop]`; **Ani → drop**; borjan stays flag-mode (filter_notes still judgment-drops) so his resolved hard_filters are byte-identical (verified: no new toggle keys). **(2) Over-constraint telemetry** — every run now records `funnel_in` (candidates that reached filtering), `drop_by_param` (counts by cause across hard_filter/stale/unverified/work_arrangement/employment/language), and `drop_nudges`; when one param drops ≥40% of the funnel the ledger prints `⚠ over-constraint: <param> dropped N/M (X%)`. Informs, never auto-relaxes (Ani's remote-only legitimately drops 168/319=53% — nudge fires, correctly). Written to runs.json + candidates JSON. **(3) Diagnosed borjan's "0 new"** — NOT a mechanical over-drop: his last scan produced 80 candidates / 1 drop; the 0 was the judgment layer declining all 80 (US-only/PL-hybrid/Polish/auth-walled) — a thin market, now made visible by the telemetry. **(4) Audit doc** [ENFORCEMENT_COVERAGE.md](ENFORCEMENT_COVERAGE.md) maps every declared param → enforcement strength (drop/flag/assess), flags the one remaining gap (`target_seniority.strict` still judgment-only), and sets the opt-in-toggle pattern for future/custom onboarding params. Skill `job-scout-run`→**4.6.0**. `validate-platform` green; all 8 plans resolve; detector unit-tested (en/pl/de). borjan-pm resolved-config byte-identical; no PII. |
| 2026-07-19 | Ani first-run scanner leak fix + feedback capture (Opus 4.8, branch `claude/ani-project-setup-2rb1b0`) | Ani's Part-B companion dogfood surfaced a real scanner leak: full-time, Polish, and **hybrid** roles reached her `New — Unreviewed` despite remote-only + part-time config ("something is leaking somewhere"). **Forensics on the real 319-candidate run:** (1) `work_model: [remote]` was **declared but unenforced** — 168/319 carried `(hybrid)` in the loc tag, no detector read it (the `filter_notes` lesson was never wired to code); (2) full-time-on-B2B **swallowed** — `not (detected & accept)` let `full_time` pass whenever `contract`/`b2b` co-occurred (36 roles; time-commitment ≠ contract-vehicle); (3) Polish leaked via **JD-less dedup twins** + title-only Polish; (4) the run then **shortlisted flagged candidates on fit alone**. **Fix (additive, profile-gated):** `core/scan.py` gains `detect_work_arrangement(loc,jd)` (structured loc tag authoritative over body prose), a part-time guard in employment detection (explicit `full_time` without `part_time` is off-target even on B2B/contract), and a title-level Polish fallback; new opt-in `hard_filters.work_arrangement: [off\|flag\|drop]` + `employment_mismatch: [flag\|drop]` compiled only when non-default; a post-annotation drop partition routes the machine-certain hard-eligibility misses to seen.jsonl `Filtered Out` (recoverable), same class as `us_only`. Ani opts into `drop`; **borjan-pm resolved-config hash is byte-identical before/after** (verified by stash A/B) since it doesn't set the toggles even though it shares `work_model:[remote]`. Skill `job-scout-run` → **4.5.0**: documents the new flag + drop modes and hardens "resolve EVERY flag before shortlisting" (issue #4). Schema + `_validate` + `_compile_hard_filters` updated. **Verified:** `validate-platform` green; ani `--plan` resolves; replay over the real run drops 190/319 (168 hybrid + 22 full-time) and keeps the genuinely-remote roles. **Feedback banked** in [ANI_FIRST_RUN_FEEDBACK.md](ANI_FIRST_RUN_FEEDBACK.md): companion-doctrine folds (voice Q&A worked-examples + selectable options, gender/feminine dial, numbered-skippable phased voice generation, save-is-default, always-clickable-link, proceed-as-options) as the NEXT PR, and app UX (sliders for English-level/complexity/formality, tap-to-edit KB, salary net/gross calc) as roadmap. borjan-pm untouched; no PII; bindings still compose-clean (toggles aren't in the snapshot). |
| 2026-07-19 | Finding #13 — Passed/Seen resolution read-back (Opus 4.8, branch `claude/pr-40-autonomy-question-7wd2z6`) | Surfaced by the 2026-07-19 two-profile parallel live scan: 16 borjan-pm rows were resolved in Notion (Declined/Filtered/Stale) but still `shortlisted` in local `seen.jsonl` — the mirror gap of #8/#11 (reconcile back-fills `applied` from the Tracker but never read Passed/Seen resolutions back, so local state drifts: stale counts + the sweep keeps re-checking resolved rows). **Fix:** new `reconcile_resolutions_from_passed_seen` — READS the Passed/Seen Log and maps any still-`shortlisted` local record whose live Notion reason is a resolution to the matching local status (`_RESOLVED_LOCAL_STATUS`: User Applied Elsewhere→applied, User Declined→passed, Filtered Out/Duplicate/Stale→dropped; New — Unreviewed/Unverified/Blocked skipped as still-live). Read-only on Notion, token-gated, idempotent. Wired into the scan-start reconcile (`scan.py`) + the `--reconcile` CLI (now emits `{tracker, resolutions}`). Offline-verified (resolves a Declined row, leaves a New — Unreviewed row, idempotent on re-run); `validate-platform` green. Also logged the parallel-run subagent scratch-collision into PROJECT_PLAN §Phase 5 (per-run isolated scratch dirs). **Parallel scan itself:** ran correctly, 0 shortlisted both profiles for legitimate reasons (borjan: US-only/PL-hybrid/Polish-only/auth-walled; ani: part_time filter vs full-time-dominated Java market); no rate-limits; a few hosts 403-blocked (fetch-health, not rate-limit). |
| 2026-07-19 | Post-3a follow-ups #12 + #8/#11 (Opus 4.8, branch `claude/pr-40-autonomy-question-7wd2z6`) | Closed two tracked findings after PR #40 merged. **#12 gambling mechanical hard-drop:** added a new `hard_filters.role_hard_drop_terms` field (kept separate from `role_exclusion_terms` so the flag-not-drop contract and `excl_business_analyst` are untouched). One term now has two effects — a **TITLE-scoped** mechanical auto-drop (`auto_drop_title_patterns` → `harddrop_*`, matched against the title alone in `scan.hard_filter`) plus the ordinary body **flag** (`excl_*`), so "PM Senior (lottery)" drops while a JD-body betting-client mention stays a judgment call (no silent false-drop). Moved borjan-pm's 6 gambling terms into the new field; flag_patterns + auto_drop_patterns sets byte-identical, only the new title layer added; compose snapshot excludes exclusion terms so bindings stay compose-clean (no recompose). **#8/#11 applied-lingering:** new guarded `flip_passed_seen_to_applied` (New — Unreviewed → `User Applied Elsewhere`, read-before-write guard = no clobber, idempotent). Wired into `sync_applied` (immediate flip on chat apply) **and** `reconcile_applied_from_tracker` as the catch-all net (flips any matched-but-still-`New` shortlist row → covers a companion apply whose flip was missed + back-heals pre-fix lingerers like OSF). Writes the Passed/Seen Log — scanner's own target — so the Tracker firewall is intact; still token-gated (tokenless = honest skip, unchanged). Docs: ARCHITECTURE §5 gap note → closed; schema + profile comments updated. `validate-platform` green; title-drop/body-flag + flip-guard behavior unit-checked offline. **Still open:** ani portability run; first-real-apply confirmation of the NEW-Tracker-create → back-fill box. |
| 2026-07-18 | Phase 3a.7 supervised live acceptance + lessons + strategy capture (Opus 4.8, branch `claude/job-scout-phase-3a-build-qm8xy0`) | Ran the **live 3a.7 acceptance with Borjan** end-to-end (Claude Desktop/Cowork substrate, file access). **PASSED substantively** (see the 3a.7 row). Fixes shipped mid-run: **PR #31** `scan.py --verbose`; **PR #33** the binding defect — the 31k `project-instructions.md` truncates in the ~4k claude.ai custom-instructions field, so `compose_assistant.py` now emits a compact `project-bootstrap.md` (→ custom instructions) + full doc (→ Project knowledge); **PR #39** the form-is-ground-truth verification lesson + E5 proven. Strategy/roadmap captured (Borjan's asks): **GUIDED-FLOW.md** curated prompt library + fold-in-per-step standing rule (PRs #34/#36); **HEALTH_MONITORING.md** Layer 1.5 self-healing + **Layer 2-runtime** (in-app runtime healing) + the 2026-07-18 degradation hit-list; **BUSINESS_NOTES.md** (valuation/GTM) + **BUILD_AND_FLIP_PLAYBOOK.md** (reusable method + starter-kit inventory) + Phase 6 GTM + AI-provider/BYO-key seam (ARCH §7b) (PRs #35/#37). **This wrap-up commit:** gambling/lottery hard-boundary added to borjan-pm (role_exclusion_terms → excl_* flags + a filter_note enforcing hard-drop; pure mechanical `auto_drop_terms` wiring deferred — task #12); PROGRESS 3a.7 marked passed. **OPEN for next session:** (1) finish **3a.8** (CI: composer-clean + no-PII denylist + lints), **3a.9** (docs: ARCH §5, PROFILE_CONFIG_SPEC), **3a.10** (PLATFORM_GUIDE refresh); (2) **ani portability** run (not yet done); (3) confirm the one un-tested box (companion NEW-Tracker-create → next-scan back-fill) on first real apply; (4) findings **#8/#11** (scanner hygiene: chat-apply flow + reconciliation don't flip the Notion Passed/Seen row → applied roles linger `New — Unreviewed`; OSF was the case) and **#12** (gambling mechanical auto_drop wiring). borjan-pm resolves + validates; `validate-platform` green. |
| 2026-07-18 | Phase 3a QA start + scanner observability + health-monitoring roadmap capture (Opus 4.8, same branch) | During the supervised 3a.7 acceptance kick-off Borjan asked (1) for real-time scan progress and (2) how the app self-monitors platform/core health long-term. **(1)** Added an opt-in `core/scan.py --verbose/-v` flag streaming per-phase + per-platform progress to **stderr** (stdout ledger byte-identical; default-off so unattended runs unchanged) — **PR #31 merged**. **(2)** Captured **Platform Health & Self-Healing** early per Borjan's ask ("fully build this before 3b … here early so it gets clearly buffed as we move in higher phases"): wrote the living seed **[HEALTH_MONITORING.md](HEALTH_MONITORING.md)** (honest-failure floor already in place; Layer-1 `core/health.py` emits trend/severity signals — `DOWN_STREAK`/`YIELD_COLLAPSE`/`SELECTOR_SUSPECT`/`NEVER_PRODUCED`/`SYSTEMIC` — from existing `runs.json`+`fetch_evidence.jsonl`; Layer-2 scheduled Claude health review diagnoses + repairs the catalog via CI; `health_review_due` counter reuses the recompute hook; cross-cutting, reinforced each phase) and **scheduled it as a full build between 3a and 3b** in PROJECT_PLAN §3 (Phase 3 interstitial). Prime directive intact (verbose is additive stderr; health repairs land in catalog through the validator). **3a.7 live acceptance still pending Borjan's supervised loop.** |
| 2026-07-18 | Phase 3a BUILD steps 3a.1–3a.6 + 3a.7 prep (Opus 4.8, branch `claude/job-scout-phase-3a-build-qm8xy0`) | Executed the 3a build per PHASE_3A_PLAN §11.2. **3a.1** `assistant/` generic package — 8 composable modules (`00-…07-*.md`) + README, generic doctrine extracted from `pitching.md`: overview/boundaries, consent + data principle (two retention classes, both-stores deletion), voice acquisition (honest meter, manual re-save), KB + growth loop, the apply loop, the **pinned** Notion write contract, verification (D10), voice/delivery discipline. **3a.2** profile-side split — `profiles/borjan-pm/assistant/voice-seed.md` + `data-manifest.md`; `references/pitching.md` + `gemini-prompt.md` left in place (pitch router still resolves — verified); `voice-seed.md` optional (ani → guided-Q&A). **3a.3** `core/compose_assistant.py` — deterministic/idempotent/PII-free composer (snapshot = floor/eligibility/location/Notion IDs only; no display_name/CV facts), compose-date + config-hash stamp, `--all` skips dry-run, `--check` for CI, handles absent voice-seed/manifest. **3a.4 (the ONE permitted scanner change)** — `reconcile_applied_from_tracker` (scan-start Tracker READ → seen.jsonl `applied` back-fill; token-gated, idempotent, read-only; sequenced before dedup+sweep) + read-before-write guard in `apply_sweep_update` (flip only `New — Unreviewed`, never clobber companion-resolved rows); offline simulation proves back-fill + idempotency + guard both ways; run skill v4.4.0. **3a.5** `assistant/DRY-RUN.md` — concrete two-conversation persistence round-trip script (voice meter + KB growth → save → fresh convo retrieves both). **3a.6** `assistant/SETUP.md` — full binding walkthrough incl. pinned-select-option verification (cross-checked vs `provision_notion` constants). **3a.7 prep** — composed + committed both bindings (borjan-pm + ani-backend-java) and wrote **[PHASE_3A_ACCEPTANCE.md](PHASE_3A_ACCEPTANCE.md)**, the exact supervised-live-loop runbook. **Prime directive held:** borjan-pm resolves **byte-identical** (`--plan` sha + resolved-config hash unchanged), scanner behavior unchanged when tokenless, Tracker firewall intact (reconciliation is a READ); no CV/PII in the repo. `validate-platform` green; `compose --check` clean. No `high`→`xhigh` auto-escalation needed — the amended specs covered every case. **STOPPED at 3a.7 (acceptance is Borjan's live loop); 3a.8 (CI) / 3a.9 (docs) / 3a.10 (guide) follow after acceptance.** |
| 2026-07-18 | Phase 3a §11.2 effort retier (Fable 5, branch `claude/phase-3a-review-w3gcdk` restarted off merged main after PR #28) | Post-gate retier of the build effort tiering in PHASE_3A_PLAN §11.2, per Borjan's ask: 3a.4/3a.5 drop `xhigh` → **`high` + a named auto-escalation rule** — the gate turned both from open design into precise specs, so the build runs them at `high` and **auto-switches the step to `xhigh` the moment anything surfaces that the amended spec doesn't cover** (back-fill idempotency edge, tokenless-path surprise, sweep-ordering interaction, Notion 429/permission behavior, persistence-mechanic gap) — never improvise past the spec at `high`. 3a.7 stays `xhigh` (live judgment). Fable 5 escalation kept as the last resort. Docs only; scanner + `borjan-pm` untouched. |
| 2026-07-17 | Phase 3a step 3a.0 — pre-build review gate (Fable 5 @ xhigh, branch `claude/phase-3a-review-w3gcdk`) | Ran the scoped adversarial review per PHASE_3A_PLAN §11.1 — read the full contract set (PROGRESS, PHASE_3A_PLAN, PROJECT_PLAN §1/§1a/§3, ARCHITECTURE §5, PROFILE_CONFIG_SPEC, borjan-pm `pitching.md`), spot-checked the run skill, and **verified the load-bearing claims against the engine code** (scan.py/sweep.py/notion_sync.py/dedup.py/provision_notion.py). **8 findings + the D13 ⚑ ruling, all amended into the plan (D1–D16 intact).** Blocking: (1) the D8 reconciliation **did not exist** — the scanner never reads Notion at scan start (`notion_sync.py` push-only; `applied` back-fill chat-flow-only), so 3a.4 re-scoped from "verify" to BUILD (token-gated scan-start Tracker read → `seen.jsonl` back-fill, sequenced before the sweep, honest tokenless skip) and §0a re-worded to name it the one permitted additive scanner change; (2) sweep-clobber collision — sweep scope is `seen.jsonl`-only and `apply_sweep_update` blind-PATCHes, so a companion-resolved row (esp. `User Declined`, which has no Tracker row) would be flipped to `Stale/Expired` on posting death → read-before-write sweep guard added to 3a.4. High: (3) claude.ai Projects have no programmatic knowledge write — manual voice/KB re-save mechanic made explicit (§3/§4), 3a.5 dry-run must round-trip across two conversations. Moderate: (4) companion Passed/Seen select values pinned to the provisioned vocabulary (`User Applied Elsewhere`/`User Declined`); (5) composer compose-date + config-hash stamp + re-compose triggers (snapshot drift); (6) `references/pitching.md` stays in place intact (run-skill pitch router — prime directive). Minor: (7) 3a.8 no-PII check defined as a denylist (2.7 writeback guard not reusable); (8) composer handles a voice-seed-less profile (Ani). **D13 ⚑ resolved: Project-knowledge / Notion-state split CONFIRMED** — Q&A log stays in Notion, KB is the draft-time retrieval source, deletion story names both stores. Seeded the 3a build checklist above. Docs only — no code changed; scanner + `borjan-pm` untouched. **NEXT: the Opus 4.8 build (3a.1–3a.10).** |
| 2026-07-13 | Phase 0 (cloud, branch `claude/job-scout-engine-abstraction-2g5b0o`) | Wrote the four-doc planning set; seeded Phase 1 checklist; PR #8 merged |
| 2026-07-13 | Phase 1 build (same session/branch, restarted from main) | Built steps 1.1–1.11 end-to-end: catalog, templates (PM + fe-react), profiles (borjan-pm + demo), loader+validator, core extraction, sweep, generic skill, state migration, parity (config diff + live back-to-back runs), CI. Cutover (1.12) deliberately left for Borjan. Env notes: selectolax pip-installed; playwright not needed (`--no-headless` runs); `REQUESTS_CA_BUNDLE=/root/.ccr/ca-bundle.crt` required for live fetches in cloud sessions |
| 2026-07-13 | Phase 1 merge + cutover prep | PR #9 merged. Hit a self-inflicted merge block: the CI change renamed the job that `main`'s ruleset required by exact name (`Validate job-scout-pm`) → required check went permanently "expected". Fixed by renaming the legacy job back to the exact required name (comment added so it's not re-renamed). Borjan then added an admin bypass + the `Validate platform` required check to the ruleset (permanent guard). Cutover prep PR #10 merged: references copied into the profile + CUTOVER.md runbook. |
| 2026-07-14 | Post-cutover: lessons feedback + legacy freeze (branch `claude/job-scout-platform-resume-mfdp2r`) | Fed the first-attended-run lessons back into the engine + skill (v4.1.0): computed candidate annotations in `core/scan.py` (`non_english_jd` Polish-only-JD detector, `start_date_passed` stated-start-in-past detector, `missing_company` data-quality flag) and `core/dedup.py` helpers (`company_index`, `role_family`) driving `company_prior` + `applied_variant_saturation` (location-agnostic country-clone cross-check). Skill: non-English-JD hard filter, prior-history surfacing, Notion page-ID caching (kills the query→flip 429s), memory-ID re-verification rule, honesty-check step in the pitch router. Profile: JJ.it Polish-only filter_note + catalog quirk; `pitching.md` got the salary-ask heuristic, an explicit gap-honesty procedure, and a (Borjan-to-confirm) intro/links block. **Legacy freeze:** `job-scout-pm/README.md` archive note; parity CI step retired from `validate-platform` (tiers now diverge by design post-recompute — `parity_diff.py` kept as historical). validate-legacy CI job + ruleset swap deliberately deferred to avoid the required-check trap. |
| 2026-07-15 | Phase 2 brainstorm + execution plan (branch `claude/job-scout-phase-2-plan-kq3i6m`) | Interactive design session (Opus 4.8) with Borjan. Settled Phase 2 as **broad template groundwork + CV-driven setup templater + Notion provisioning**. Locked 13 decisions (D1–D13): option-C breadth (templates + catalog expansion, new boards as `status: unverified`); one cross-pollinating catalog with **per-stream tier rotation** (catalog=capability, template=tier order); CV read-not-stored templater with guardrails (reweight/select + obvious tech terms, no inventing types); opt-in human-reviewed write-back loop; `target_seniority` (soft + `strict`) orthogonal to role; `employment_type` (hard + `any` escape); `run.effort` model-tier (entitlement-shaped, two-stage design, **design-and-defer** with scheduling); provisioning token/parent as params (Borjan's Notion for trial, per-user end goal); scheduling deferred (config-only). Expanded taxonomy to ~12 streams incl ai-ml, web-dev, game-dev, tvos/android-tv, it-support, content-writing, SE-leadership subvariants. First test = senior Java backend eng → mid/medior part-time, supervised. A second round refined the templater/provisioning/comp threads (**D14–D22**): user-chosen interview posture (progressive vs full); primary+optional-secondary subvariant per profile; CV-optional (Q&A fallback); Notion access = instruct→verify-by-probe; **encrypted-at-rest secret-storage seam** (token captured once, never in repo; toward a full account/connector one-time setup); idempotent provisioning marker; **no shipped salary numbers** (heuristics text only, floor user-provided/unset); core seniority lexicon + template extensions; part-time floor pro-rated by `fte_fraction`. Wrote **[PHASE_2_PLAN.md](PHASE_2_PLAN.md)** (22-decision design contract + ordered 2.1–2.11 checklist); seeded the Phase 2 table above; flagged the next session as the build phase. Added the **model/effort handoff (§12)**: a scoped Fable 5 pre-build review gate (step 2.0, xhigh) followed by the Opus 4.8 build with per-step effort tiering (medium/high/xhigh). No code changed — planning only. |
| 2026-07-16 | Phase 2 step 2.0 — pre-build review gate (Fable 5 @ xhigh, branch `claude/phase-2-plan-review-uxmbdj`) | Ran the scoped adversarial review per plan §12.1. **4 blocking findings, all amended into PHASE_2_PLAN.md (D1–D23 untouched):** (1) new catalog boards must ship `active: false` + `status: unverified` with a loader consistency rule — a bare active entry would fail every existing profile's tier-coverage validation (invariant #14), breaking `borjan-pm` at 2.3; (2) *enforcement* of the new fields (salary.py FTE pro-rating + floorless handling; run-skill `target_seniority`/`employment_type` semantics via the lexicon) was assigned to no step while 2.9's acceptance depends on it — now explicit scope in 2.1/2.2 via a §3.1 enforcement mapping; (3) floorless compensation contract specified (floor optional, sub-keys required-when-present, below-floor first-pass disabled + judgment estimation when unset); (4) `search.subvariant` was mislabeled "existing" — it is NEW, added to the 2.1 field list. 6 non-blocking findings recorded in plan §12.1's gate-outcome note + the gate PR. Also fixed this file's Phase 2 header under-count (D1–D13 → D1–D23), the plan header's build-model mislabel (build = Opus 4.8 per §12.2), and re-pointed the NEXT banner at the build. **Second pass (same session, after the blocking-fix PR #16 merged):** Borjan clarified he wanted ALL findings fixed — the 6 non-blocking amendments applied in a follow-up PR: §6 write-back staging moved to a top-level `suggestions/` dir (the template CI glob strict-validates everything under `templates/`); §3.3 `platform_tiers` = template-only block, interview-time seeding, never loader-merged, deterministic rule for unlisted actives; §9 2.4/2.10 dry-run fixture profile per ⭐ template driving the CI `--plan` smoke; §5 Notion view-creation API probed first with a D17 instruct→verify fallback; §3.1 `employment_models` ↔ `employment_type` token mapping + contradiction warning; §3.1/§8 `medior` dropped from the band enum (D21 lexicon title → `mid`). All 10 findings now incorporated. Docs only — no code changed. |
| 2026-07-16 | Phase 2 step 2.1 — schema extensions + mechanical wiring (Opus 4.8 @ high, branch `claude/job-scout-phase-2-slrlym`) | First build step. Added the Phase 2 schema fields additively: `search.subvariant`/`subvariant_secondary` (NEW), `search.target_seniority{bands,strict}` (D7), `search.employment_type{accept}` (D8, `any`-exclusive), `compensation.fte_fraction` (D22), optional `compensation.floor` (D20 floorless contract), and the new top-level `run{effort,effort_by_run_type}` (D9/D10). Wiring per the §3.1 enforcement mapping: `salary.normalize_floor` gained floorless (returns `canonical_gross_month: null` → `assess()` unparseable, never a drop) + FTE pro-rating (3012→1506 at 0.5 FTE); loader `_validate` enforces all new enums/ranges + the floorless floor contract (gross_net_ratio required only with a floor); a named non-fatal load **warning** on employment_type↔eligibility contradictions; `scan.py --plan` prints the floorless/pro-rated cases. Updated `core/schema/profile.schema.yaml` + PROFILE_CONFIG_SPEC §2/§3/§5. **Prime directive held:** `borjan-pm` resolved config behaviorally identical (canonical floor unchanged, only additive `fte_fraction: null` key), `--plan` intact, `validate-platform` green. `run.effort` recorded only — model-selection wiring deferred (D10). |
| 2026-07-16 | Phase 2 step 2.2 — template format v2 + judgment-layer wiring (Opus 4.8 @ high, same branch) | New core artifact `core/data/seniority_lexicon.yaml` — base title→band lexicon (D21), 36 entries incl. medior→mid, staff/principal/lead. Loader: `_TEMPLATE_KEYS` gains `platform_tiers` + `seniority_titles` (both template-only; `platform_tiers` is a seed for the interview, NEVER loader-merged per §3.3); extends-chain inherits them (seniority_titles deep-merged); `load()` resolves `cfg["seniority_lexicon"]` = base + template `seniority_titles`. `scan.py`: `detect_seniority` (word-boundary, longest-key-wins) + `detect_employment` detectors and guarded per-candidate annotations `seniority_detected`/`seniority_off_target` (D7) + `employment_detected`/`employment_off_target` (D8) — additive, fire ONLY when the profile sets the field. `skills/job-scout-run/SKILL.md` → v4.2.0: Hard-Filters rows for D7 (soft scoring / `strict` hard-drop) + D8 (accept-set, `any` escape) + floorless (D20) / FTE-pro-rated (D22) salary judgment; annotation list + Files + changelog updated. Both existing templates (delivery-manager, frontend-react) carry `platform_tiers` + `seniority_titles`. Spec §6 documents the v2 blocks. **Prime directive held:** borjan-pm resolved config behaviorally identical (only additive `seniority_lexicon` top-level + `fte_fraction` metadata keys; scan annotations no-op with no target fields), `--plan` intact, `validate-platform` green. |
| 2026-07-16 | Phase 2 step 2.3 — catalog expansion (Opus 4.8 @ high, same branch) | Loader `status` rule FIRST in `load_catalog` (`status: unverified ⇒ active: false`, invalid status rejected) so new boards stay out of tier-coverage (invariant #14) until a live smoke test flips `verified`+`active` together — keeps every existing profile validating unchanged. Extended per-stream category slugs to the new taxonomy streams on the 6 general aggregators (wwr/himalayas/remotive/working-nomads/nodesk/remote-ok), each marked `categories_verify_at_setup`; himalayas also got per-stream prefilter phrases for ai-ml/data/design-ux. Added 5 researched stream-appropriate boards — ai-jobs.net (ai-ml/data), Dribbble + Behance (design-ux), ProBlogger (content-writing), icrunchdata (data/ai-ml) — all `active:false`+`status:unverified`, honestly marked "researched, NOT smoke-tested". Catalog 23→28 entries. Per-stream tiers already live in templates (2.2). **Prime directive held:** borjan-pm's active rotation is bit-identical (21 platforms, same URLs/tiers/filters); the only diffs are additive inactive boards + one clarifying quirk sentence on remote-ok. `validate-platform` green. |
| 2026-07-16 | Phase 2 step 2.4 — template library (Opus 4.8 @ medium, same branch) | Built 20 new templates (22 total) across the §2 taxonomy following the frontend-react/delivery-manager pattern; new templates ship NO floor numbers (D20 — `salary_estimation_heuristics` text only), plus `platform_tiers` + `seniority_titles` v2 blocks. **⭐ tech-core deepest:** backend-java (the live-test case, full keyword/archetype/band/heuristic depth + Java-specific seniority_titles) with dry-run fixture `demo-backend-java` exercising the whole D7/D8/D20/D22 chain (target mid + part_time/contract + fte_fraction 0.5 + floorless). Flagship + dry-run fixture per ⭐ stream: ai-engineer (`demo-ai-engineer`), automation-sdet (`demo-automation-sdet`), data-engineer (`demo-data-engineer`), product-designer (`demo-product-designer`) — 5 new fixtures, all `--plan` clean, driving the 2.10 CI smoke. SE depth: backend-node/python, fullstack-js, devops-sre-platform, engineering-manager (leadership as its own subvariant per D7). Extra ⭐ subvariants: ml-engineer, data-analyst, qa/manual. PM breadth: scrum-master, product-manager (both stream `project-management`). Business/support/content groundwork (marketing/growth, sales/account-executive, people-hr/recruiter-talent, it-support/technical-support, content-writing/technical-writer), honestly marked catalog-coverage-pending. Every §2 STREAM now has ≥1 template + every ⭐ stream a flagship+fixture; the remaining subvariant enumeration is documented backlog to fill as streams are dogfooded. **Prime directive held:** borjan-pm validates + `--plan` identical (21 platforms, floor 3012.05). `validate-platform` green (22 templates + 7 profiles). |
| 2026-07-16 | Phase 2 steps 2.8 + 2.7 (Opus 4.8, same branch) | **2.8 effort/model tier** (design-and-defer): documented the mapping (fast→Haiku/mid→Sonnet/high→Opus), two-stage judgment design (cheap triage → capable shortlist read via subagent) and entitlement shape in run skill v4.3.0 + ARCHITECTURE §7a; schema field already in 2.1, no wiring (D10). **2.7 write-back loop**: `core/writeback.py` — consent-gated (`writeback.consent`, D6), generic-only `stage_suggestions` with a PII guard (refuses emails/URLs/sentences/anything not a short generic term) + provenance/frequency upsert, and `validate_suggestion_file` (the light CI check 2.10 will wire in); new top-level `suggestions/` dir (deliberately OUTSIDE `templates/` so the template CI glob doesn't strict-validate it) with a README (format, curator flow, PII invariant); optional `writeback.consent` schema field. Self-test confirms generic terms stage and PII is refused. Both steps Notion/CV-independent (built while awaiting Borjan's token + CV for 2.5/2.9). borjan-pm identical throughout; `validate-platform` green. |
| 2026-07-16 | Phase 2 steps 2.5 + 2.6 + 2.9 + 2.10 + 2.11 — provisioning, setup skill, live backend-java test, CI, docs (Opus 4.8 @ xhigh, branch `claude/job-scout-phase-2-slrlym` restarted from merged main after PR #18) | **2.5** `core/provision_notion.py` (+`secrets.py` D18 seam): probed the Notion API FIRST (parent reachable via token; saved-view creation NOT in REST → D17 instruct→verify); provision/adopt on the 2025-09-03 data-sources API, idempotent-by-title (D19). **2.6** `skills/job-scout-setup/SKILL.md` v1.0.0 — the CV-driven interview/templater. **2.9 LIVE TEST (D13 acceptance PASSED):** onboarded **Ani Petreska** (CV read in-session, never stored per D4) → Senior Java/Spring Boot → `backend-java`; created `profiles/ani-backend-java`; **provisioned 3 real DBs live under Borjan's Test-Run parent page** (idempotent re-run adopted, 0 dupes); validated; **live scan: 94 candidates, 13 covered / 9 sources down (honest ledger), sweep ran.** Semantics behaved: 44/46 senior+ titles soft-flagged `seniority_off_target` (mid targeted, strict:false → deprioritize-with-note not drop), contract/b2b honored + full-time-only flagged (D8), 0 salary auto-drops (floorless D20). **2.10** wired the `suggestions/` staging CI check into validate.py (per-⭐ `--plan` smoke already covered by the ci.yml profile loop + 2.4 fixtures). **2.11** docs (ARCHITECTURE layout+§7a, PROFILE_CONFIG_SPEC, PROGRESS). **Prime directive intact:** zero changes to borjan-pm config/state/Notion; ani onboarding is purely additive. **Lessons:** (1) JustJoin.it resolves backend-java via the generic SE slug `javascript`/cat-id 1 — a Java-specific category slug would sharpen precision (subvariant refinement, verify_at_setup). (2) The Java market skews senior — Ani's mid target soft-surfaces mostly senior roles with a note, validating the soft-seniority design (she still sees them). (3) non_english_jd×4 (Polish JDs on JustJoin) — the borjan lesson generalizes across profiles. (4) Cloud-IP bot-blocking downs the headless boards (Arc/RR/NoDesk/Dynamite/WttJ/Landing/JustRemote/Crossover) — the local lane covers them; honest `sources down` ledger did its job. |
| 2026-07-17 | Post-onboarding: Ani judgment pass + dogfood improvements (Opus 4.8, branch `claude/job-scout-phase-2-slrlym` off merged main) | **Judgment pass on Ani's 94 candidates** → 2 scored Java shortlist rows synced to her Notion 📥 New — Unreviewed (PradeepIT Core-Java/Spring/microservices 7.2; CoorB Java/banking 7.0, US-location-verify), 21 stale drops to the full log, digest surfacing the LinkedIn login-walled Java leads (Luxoft/Kainos/UST/ilionx/Nord-Security-Mid). **Improvements into repo:** (1) Ani `filter_notes` — real-run lessons (Kraków-hybrid≠remote-for-Skopje; LinkedIn richest mid-Java but login-walled; US-tagged Himalayas Java needs remote-eligibility verify; Java market skews senior→soft-surface expected). (2) **Tier-honesty fix** — the 5 business/support template `platform_tiers` (marketing/sales/people-hr/it-support/content-writing) were listing tech-only boards that lack the stream slug (skipped-honest at scan time); trimmed to real coverage (stream-slug boards + ATS) so seeded tiers no longer overstate — verified 0 slug-less. **Confirmed for the record (answers to Borjan's Q):** per-stream tier ROTATION works (D3 — e.g. `wwr` = SE T2 / PM T3; `arc` = SE T2 / PM T3; `justjoin-it` T1 for SE/PM, absent for marketing/sales); all template tiers populate T1/T2/T3 (not just T1); the new niche boards (dribbble/behance/problogger/ai-jobs/icrunchdata) are `active:false`+`unverified` and stream-scoped, so they do NOT leak into engineering scans (and won't until a smoke test activates them). **Coverage TODOs (dogfood):** qa (⭐) has only remotive+ATS and ai-ml is thin — add stream slugs/aliases (e.g. qa→eng category) when those streams are dogfooded; backend-java resolves JustJoin via the generic SE `javascript` slug/cat-id 1 — add a Java-specific category (verify_at_setup) to sharpen precision. borjan-pm untouched; `validate-platform` green. |
| 2026-07-17 | Dogfood TODOs: JustJoin Java category + qa/ai-ml coverage (Opus 4.8, off merged main) | **(TODO 2) backend-java JustJoin precision:** probed the JustJoin by-cursor API live — category id 1 is JavaScript (TS/frontend/node), Java/Kotlin is **id 6**; backend-java was fetching JS then title-filtering (thin Java yield). Restructured JustJoin `category_ids` to be keyed by the resolved category SLUG (`pm:15, javascript:1, java:6`) and made the loader resolve `category_id` by the resolved slug (subvariant override first, stream key fallback — backward-compatible); backend-java template now sets `platform_slugs: {justjoin-it: java}` so Ani + the fixture enumerate `java`/6. **borjan-pm (pm/15) + demo-fe-react (javascript/1) byte-identical.** **(TODO 1) qa/ai-ml coverage:** added stream-slug aliases to the aggregators (wwr/himalayas/working-nomads/nodesk/remote-ok — QA→their eng category, AI-ML→data/programming; all `categories_verify_at_setup`); qa 5→10 boards, ai-ml 6→10; trimmed the 4 qa/ai-ml template tiers to honest coverage (0 slug-less). Niche boards (ai-jobs.net/icrunchdata) stay catalog-unverified until a smoke test. `validate-platform` green; borjan-pm untouched. |
| 2026-07-17 | Niche-board smoke test + per-stream board gating (Opus 4.8, off merged main) | **Smoke-tested the 5 unverified niche boards live:** ai-jobs.net (50 postings), icrunchdata (15), dribbble (48) are clean SSR HTML → **activated** (`status: verified` + `active: true` + generic HARVEST_SPECs added, harvest confirmed end-to-end via the scan path); behance (HTTP 403 anonymous) + problogger (WPJobBoard JS plugin) stay unverified with honest quirks (need headless/custom fetchers). **Engine: proper per-stream board gating** — a board that DECLARES categories now serves ONLY those streams (loader resolver + validator tier-coverage both updated): a stream-specific board can be catalog-active without being fetched for unrelated streams or forced into every profile's tiers. So the 3 new boards serve ai-ml/data (ai-jobs.net, icrunchdata) + design-ux (dribbble) only. Tiered into the ai-ml/data/design templates + their demo fixtures. **Prime directive:** borjan-pm/ani/demo-fe-react resolve **byte-identical (21 active, none of the new boards)** — the gating keeps them out; borjan-pm.yaml untouched. Side effect (more correct): the ai-ml/design demos now gate to their real coverage (were over-fetching non-stream boards). `validate-platform` green. **Readiness note:** config-readiness of the other streams is now high (complete templates, honest tiers, improved catalog coverage), but battle-tested parity with borjan-pm still requires per-profile dogfooding (live scans + learned filter_notes + conversion-tuned tiers) — that's incremental per real onboarding (Ani is the first). Remaining uplift: live-verify the aggregators' per-stream slugs (still verify_at_setup) and headless fetchers for behance/problogger. |
| 2026-07-17 | Slug verification + headless fetchers + Ani re-scan (Opus 4.8, PR #24) | **(1) Aggregator slug verification:** only Remotive exposes a real category API param — verified against its live `/categories` and fixed 3 bugs (`software-dev`→`software-development` — was INVALID, SE profiles hit the unfiltered stub; `customer-support`→`customer-service`; `ai-ml`: `data`→`artificial-intelligence`); cleared Remotive's verify_at_setup. The other aggregators fetch all-category feeds (slug cosmetic, keyword-scoped) — documented, not falsely "verified". **(2) Headless fetchers:** problogger ACTIVATED (headless render surfaces WPJobBoard postings `/jobs/job/<slug>/`, ~9 live; HARVEST_SPEC + fetch_mode: headless + verified/active; content-writing template tiered). behance NOT activatable (403 direct; headless loads only the app shell, jobs arrive via async XHR that render(url)→html can't capture) — honest quirk, kept unverified. **(3) Ani re-scan (Task validates the JustJoin java/6 fix live):** 94→**319 candidates**, JustJoin 94→**260 real Java/Kotlin** roles (was JS). Judgment pass → **8 mid/regular remote Java B2B roles** in her Notion inbox (Link Group Mid, RITS Mid, BlueSoft banking, Spyrosoft energy=her utilities domain, SCALO, Synerise + scan-1's PradeepIT/CoorB); senior soft-noted, Warsaw onsite/hybrid + Polish-only JDs (×20) dropped; stream-gating correctly skipped ai-ml/data/design boards for the SE profile. borjan-pm/ani byte-identical (21 active); validate green. |
| 2026-07-17 | Phase 2 step 2.12 — human-readable documentation (Fable 5, branch `claude/phase-2-documentation-awyxah`) | Full project/repo assessment → wrote **[PLATFORM_GUIDE.md](PLATFORM_GUIDE.md)**, the plain-language guide: what the platform is, why it exists, the goal/phase map, what Phases 0–2 actually built (with the live inventory: 28-platform catalog / 25 active, 22 templates over 11 streams, 8 profiles, 18 core modules), how a scan works step-by-step, the three-layer architecture, the build method (PROGRESS contract, plan→review-gate→build, prime directive, dogfooding, honesty principle), the **Maia worked example** (React engineer in Lisbon, onboarding→daily use→Phase 3 outlook) with Ani's real onboarding as the proof, repo map + glossary. README re-pointed at the guide as the entry point. **Adopted the standing rule: every phase execution/build plan ends with a human-readable documentation step** — recorded in PROJECT_PLAN §4, appended as 2.12 to the PHASE_2_PLAN §9 checklist + this table. Docs only — no engine/config change; borjan-pm untouched. |
| 2026-07-18 | Phase 3a detailed plan (Opus 4.8, branch `claude/job-scout-phase-3a-plan` off merged main) | Wrote **[PHASE_3A_PLAN.md](PHASE_3A_PLAN.md)** — the detailed execution plan for sub-phase 3a (voice + knowledge base + the apply loop), PHASE_2_PLAN-shaped: §0a both standing principles, a D1–D16 design contract (carrying the settled apply-loop decisions D6–D11/D16 verbatim + the new companion decisions D1–D5 voice/KB, D12–D15 composer/storage/data-model/acceptance), the durable 5-entity data model (3a builds voice/KB/application-log), the voice-acquisition procedure (docs-or-Q&A, honest coverage+blind-calibration meter the user judges, voice-only-doc shredding), the KB growth loop (unknown-answer→store), the per-role apply loop, the package/binding/composer, acceptance (borjan-pm real loop + Ani portability), out-of-scope (interview=3b, CV/writing=3c, client-side store=Phase 4, integrations=Phase 5), the ordered **3a.0–3a.10** checklist, and §11 handoff (Fable 5 gate @ xhigh as 3a.0, then Opus 4.8 build + effort tiering). **One decision (D13, the substrate-(a) Project-holds-knowledge / Notion-holds-application-state split) is flagged ⚑ for the gate to stress-test** rather than assumed. Also pointed the superseded PHASE_3_PLAN.md at the 3a plan. Docs only; scanner + borjan-pm untouched. **NEXT: the 3a.0 Fable 5 review gate** (Borjan to run unattended), then the build. |
| 2026-07-17 | Phase 3 vision reframe → Application Companion (Opus 4.8, same branch `claude/job-scout-phase-3-plan-design`) | Borjan pushed back on the apply-bot plan ("too fast, ai-slop") and re-opened the brainstorm as a proper user-centric design session. Phase 3 grew into the **Application Companion**: two user-owned assets that grow with use — the **voice profile** (learned from the user's own writing, or guided Q&A; user-judged "sounds like you" meter; voice-only docs shredded after extraction, domain docs kept + deletable) and the **knowledge base** (honest answers, unknown-answer→growth loop) — feeding drafting, an interview lifecycle (prep + debrief + paste-email status), a CV doctor (+delta ingest), and a writing coach. Staged **3a/3b/3c** (3a = voice+KB+apply loop = the UAT milestone after Borjan's next scan). Adopted a co-equal **data & privacy principle** (client-side destination; Claude = engine not vault; no mining; deletable; strict GDPR; honest option-(a)-now / option-(b)-Phase-4 substrate). Per Borjan: **PROJECT_PLAN.md is the single source of truth** — folded the whole vision in (rewrote §1 Vision + new §1a principles + expanded phase map: Phase 4 = app + client-side store, Phase 5 = platform + integrations [scoped email OAuth, LinkedIn *export* never scraping] + accounts/tiers; added §3x parked-ideas so nothing is lost — Borjan's delta prompt, voice meter, email/LinkedIn, tiers; refreshed §5 open questions, product-layer Qs parked for Phase 4). Assessed past sessions for stripped vision: verbatim inputs unrecoverable (only Claude-summarized residue), but the product seeds Borjan dropped in Phase 2 (free/paid entitlement tiers, accounts/one-time-setup, per-user, encrypted secrets, Notion→real-DB storage adapter) **survived scattered** in ARCHITECTURE/D-decisions and were pulled up into the vision; the companion itself was net-new today. Marked the old apply-bot PHASE_3_PLAN.md **superseded** (its apply-loop decisions kept as 3a reference). Docs only — scanner + borjan-pm untouched. Answered Borjan's capability Qs: email auto-ingest = scoped OAuth, Phase 5 (paste now); LinkedIn = user's own data export not scraping, Phase 5; delta self-review prompt = Borjan to hand over at 3c. **NEXT: detailed 3a plan (focused brainstorm→plan→review→build), then live-scan UAT.** |
| 2026-07-17 | Phase 3 brainstorm + execution plan (branch `claude/job-scout-phase-3-plan-design`) | Interactive design session (Opus 4.8) with Borjan. Settled Phase 3 as the **Application Assistant**: a claude.ai Claude Project (+ Notion MCP) that reads a profile's `📥 New — Unreviewed` shortlist, walks it role-by-role, re-verifies facts, drafts application materials **in the user's voice** from Project-held data (CV/cover-letter bank/articles/past answers), and records applications. Locked 16 decisions (D1–D16). **The load-bearing idea: Notion is the SOLE scanner↔assistant bridge** — the assistant lives on claude.ai, cannot read repo files or run `core/*.py`, so its entire persistence is Notion; the **Tracker row is the cross-process dedup handoff** (D7) and the scanner owns local-state reconciliation (D15), so Phase 3 needs almost no core change. Key settled forks: **`pitching.md` splits along the engine/profile seam** (generic apply doctrine → `assistant/`; profile voice/facts → `profiles/<id>/assistant/voice.md`, D3); the Project is configured by a **build-time composed snapshot** via `core/compose_assistant.py`, not runtime repo reads (D4, preserves the D5 data boundary); **write-ownership partitions by row STATE** so scanner and assistant can't collide (D6); **assistant primary + Claude Code "I applied" chat fallback** as the two scanner-external Tracker writers, firewall intact (D8); **re-verify fetch-first with an honest paste/confirm fallback** for walled JDs (D10); the assistant **may set `Stale/Expired` on directly observed death** (D11); **acceptance = `borjan-pm` real supervised loop + `ani-backend-java` portability proof** (D14). Wrote **[PHASE_3_PLAN.md](PHASE_3_PLAN.md)** (16-decision design contract + ordered 3.0–3.9 checklist, ending with the standing human-readable-doc step); seeded the Phase 3 table above; flagged the next session as the build. Added the **model/effort handoff (§12)**: a scoped Fable 5 pre-build review gate (step 3.0, xhigh) then the Opus 4.8 build with per-step effort tiering (medium/high/xhigh). No code changed — planning only; the scanner + `borjan-pm` untouched (prime directive). |
| 2026-07-13 | Cutover: state lock + supervised first live run | Routines paused by Borjan. Final `migrate_state.py` = verified no-op (765=765, state already final). **First live run of core v4.0.0** on borjan-pm: 116 candidates, 18 platforms covered, 4 sources down (all known degradations), sweep correctly deferred all 18 unreviewed (<24h). **Caught + fixed a real bug**: salary period inference read monthly non-USD pay 12x too low (63 false below_floor→5 genuine) — currency-neutral EUR-magnitude inference (commit d26927b). Judgment pass under Borjan's **worldwide/EMEA-open-only** policy: 5 shortlisted, 111 dropped (logged to seen.jsonl; drops kept local this run, not pushed to Notion). **REST Notion path live-validated for the first time** (NOTION_TOKEN via env; 5 rows + digest pushed, post-write asserted). Token was shared in chat → Borjan to rotate. Remaining cutover: re-point cloud Routine (Claude via MCP) + laptop task (Borjan), then freeze legacy. |
